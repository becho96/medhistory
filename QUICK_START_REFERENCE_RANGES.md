# Быстрый старт: Референсные значения на графиках

## ✅ Что реализовано

На графиках анализов теперь отображаются:
- 🔴 **Красные пунктирные линии** — границы нормы (минимум и максимум)
- 🟢 **Зелёные точки** — значения в норме
- 🟡 **Жёлтые точки** — близко к границе нормы
- 🔴 **Красные точки** — вне нормы
- 📊 **Легенда** под графиком с расшифровкой

## 🚀 Быстрое применение (3 шага)

### 1. Применить миграцию БД (добавление поля `gender`)

```bash
cd backend
python scripts/apply_gender_migration.py
```

**Вывод должен быть:**
```
✅ Миграция успешно применена!
✓ Поле 'gender' успешно добавлено в таблицу 'users'
```

### 2. Проверить референсные значения (опционально)

```bash
cd backend
python scripts/populate_reference_ranges.py --dry-run
```

Если референсных значений нет, загрузите их:
```bash
python scripts/populate_reference_ranges.py
```

### 3. Перезапустить сервисы

```bash
# Backend (если используете docker-compose)
docker-compose restart backend

# Или если запускаете локально
# Просто перезапустите процесс backend
```

```bash
# Frontend
# Просто обновите страницу в браузере
```

## 📱 Как использовать

1. **Укажите пол в профиле:**
   - Нажмите кнопку **"Профиль"** в правом верхнем углу
   - Выберите пол (Мужской/Женский/Другой)
   - Нажмите **"Сохранить"**

2. **Посмотрите график:**
   - Перейдите в **"Анализы"**
   - Выберите любой анализ
   - На графике появятся линии границ нормы и цветовая индикация точек

## 🎨 Визуальный пример

```
График анализов (пример Гемоглобин):

Макс: 160 г/л  ━━━━━━━━━━━━━ (красная пунктирная линия)
                    ●       ● (зелёные точки - норма)
                  ●   ●   ●   
Среднее: 142 г/л ━━━━━━━━━━━━━ (зелёная пунктирная линия)
                ●           ●  (жёлтая точка - близко к границе)
Мин: 120 г/л  ━━━━━━━━━━━━━ (красная пунктирная линия)
              ●               (красная точка - вне нормы)
```

## 🔧 Что если линии не появились?

### Проверка 1: Пол указан?
```sql
SELECT id, email, gender FROM users LIMIT 5;
```
Должно быть: `gender` = `male`, `female`, или `other`

### Проверка 2: Есть ли референсные значения?
```sql
SELECT canonical_name, reference_male_min, reference_male_max
FROM analyte_standards 
WHERE canonical_name = 'Гемоглобин';
```
Должны быть числа в `reference_male_min` и `reference_male_max`

### Проверка 3: Миграция применена?
```sql
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'users' AND column_name = 'gender';
```
Должна вернуться строка с `gender`

## 📚 Полная документация

- **Подробная инструкция:** `docs/REFERENCE_RANGES_SETUP.md`
- **Сводка реализации:** `IMPLEMENTATION_SUMMARY.md`
- **План внедрения:** `docs/REFERENCE_RANGES_IMPLEMENTATION_PLAN.md`

## 🎯 Основные файлы изменений

**Backend:**
- `backend/app/models/user.py` - добавлено поле gender
- `backend/app/api/v1/endpoints/documents.py` - обновлён /labs/timeseries
- `backend/app/api/v1/endpoints/auth.py` - добавлен PATCH /auth/me

**Frontend:**
- `frontend/src/pages/Labs.tsx` - обновлён график
- `frontend/src/components/ProfileSettings.tsx` - новый компонент
- `frontend/src/components/Layout/Layout.tsx` - кнопка профиля

## ❓ Частые вопросы

**Q: Что если я не укажу пол?**  
A: Будут использоваться мужские референсные значения по умолчанию.

**Q: Можно ли указать пол для членов семьи?**  
A: Да, при создании члена семьи можно будет указать пол (функционал планируется).

**Q: Все ли анализы имеют референсные значения?**  
A: Нет, только те, которые загружены в базу данных. Для остальных линии просто не отображаются.

**Q: Можно ли изменить референсные значения?**  
A: Да, через прямое редактирование таблицы `analyte_standards` в БД.

---

**Готово!** Теперь пользователи смогут легко отслеживать, находятся ли их анализы в пределах нормы. 🎉
