# Зависимости для локального запуска бенчмарка.
# Версии выровнены с backend/requirements.txt, чтобы поведение AIService
# совпадало с тем, что в prod-контейнере.
#
# Установка:
#   cd benchmarks/document_analysis
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

pydantic[email]==2.13.4
pydantic-settings==2.13.1
PyPDF2==3.0.1
httpx>=0.27.0
PyYAML>=6.0
python-dotenv>=1.0.0

# Для команды sync-synonyms (выгрузка analyte_synonyms из локального Postgres)
psycopg[binary]>=3.1
