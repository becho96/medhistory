# 📋 Скрипты управления MedHistory

Организованная коллекция скриптов для управления локальной разработкой и продакшн-сервером.

---

## 📂 Новая структура

```
scripts/
├── config.sh          # 🔧 Конфигурация (SERVER, PATHS, COLORS)
│
├── local/             # 💻 ЛОКАЛЬНАЯ РАЗРАБОТКА
│   ├── start.sh              # Запуск Docker локально
│   ├── stop.sh               # Остановка Docker
│   ├── restart-local.sh      # Перезапуск с rebuild
│   └── README.md             # Документация локальных скриптов
│
├── prod/              # 🚀 ПРОДАКШН (Яндекс.Облако)
│   ├── deploy.sh             # Полный деплой
│   ├── update-backend.sh     # Быстрое обновление backend
│   ├── update-frontend.sh    # Быстрое обновление frontend
│   ├── update-all.sh         # Обновление всех сервисов
│   └── diagnose.sh           # Диагностика проблем
│
├── infra/             # 🏗️  ИНФРАСТРУКТУРА
│   ├── create-vm.sh          # Создание VM в Yandex Cloud
│   └── setup-server.sh       # Настройка нового сервера
│
└── utils/             # 🛠️  УНИВЕРСАЛЬНЫЕ УТИЛИТЫ
    ├── backup.sh             # Создание бэкапа
    ├── restore.sh            # Восстановление из бэкапа
    ├── status.sh             # Проверка статуса
    └── logs.sh               # Просмотр логов
```

---

## ⚙️ Конфигурация

### Файл `config.sh`

Все скрипты используют единый конфигурационный файл. Настройте его один раз:

```bash
# Редактировать настройки
nano scripts/config.sh

# Или через переменные окружения
export PROD_SERVER_IP="46.21.244.23"
export PROD_SERVER_USER="yc-user"
```

**Основные параметры:**
- `PROD_SERVER_IP` - IP адрес продакшн сервера
- `PROD_SERVER_USER` - SSH пользователь
- `REMOTE_PROJECT_DIR` - Путь к проекту на сервере (по умолчанию: `~/medhistory`)
- `REMOTE_BACKUP_DIR` - Путь к бэкапам (по умолчанию: `~/backups`)

---

## 💻 Локальная разработка

### Быстрый старт

```bash
# Запуск локального окружения
./scripts/local/start.sh

# Остановка
./scripts/local/stop.sh

# Перезапуск после изменений
./scripts/local/restart-local.sh

# Перезапуск только backend
./scripts/local/restart-local.sh backend
```

📖 **Подробнее:** [local/README.md](local/README.md)

---

## 🚀 Продакшн

### Первоначальная настройка

```bash
# 1. Создать VM в Yandex Cloud (если еще нет)
./scripts/infra/create-vm.sh

# 2. Настроить сервер
./scripts/infra/setup-server.sh

# 3. Создать .env.production в корне проекта
cp .env.example .env.production
nano .env.production  # Заполните все переменные

# 4. Первый деплой
./scripts/prod/deploy.sh
```

### Обновление приложения

```bash
# Полный деплой (долго, ~5-10 минут)
./scripts/prod/deploy.sh

# Быстрое обновление только backend (~1-2 минуты)
./scripts/prod/update-backend.sh

# Быстрое обновление только frontend (~1-2 минуты)
./scripts/prod/update-frontend.sh

# Обновление всех сервисов (~3-5 минут)
./scripts/prod/update-all.sh
```

### Диагностика

```bash
# Комплексная диагностика проблем
./scripts/prod/diagnose.sh

# Просмотр логов
ssh yc-user@46.21.244.23 'cd ~/medhistory && ./scripts/utils/logs.sh backend -f'

# Проверка статуса
ssh yc-user@46.21.244.23 'cd ~/medhistory && ./scripts/utils/status.sh'
```

---

## 🛠️ Универсальные утилиты

Работают **как локально, так и на продакшне** (автоматически определяют среду).

### Бэкапы

```bash
# Создать бэкап локально
./scripts/utils/backup.sh ./backups

# Создать бэкап на продакшне
ssh yc-user@46.21.244.23 'cd ~/medhistory && ./scripts/utils/backup.sh ~/backups'

# Восстановить из бэкапа
./scripts/utils/restore.sh ./backups/medhistory_backup_20251028_120000.tar.gz
```

### Мониторинг

```bash
# Проверка статуса (определяет среду автоматически)
./scripts/utils/status.sh

# Просмотр логов
./scripts/utils/logs.sh              # Все сервисы
./scripts/utils/logs.sh backend      # Только backend
./scripts/utils/logs.sh backend -f   # Follow режим
```

---

## 📊 Сравнение: Старая vs Новая структура

| Старая структура | Новая структура | Улучшения |
|-----------------|-----------------|-----------|
| `deploy.sh` (хардкод IP) | `prod/deploy.sh` (config.sh) | ✅ Нет хардкода |
| `update-backend.sh` (хардкод) | `prod/update-backend.sh` | ✅ Конфигурируется |
| `diagnose-registration.sh` | `prod/diagnose.sh` | ✅ Переименован |
| `backup.sh` (корень) | `utils/backup.sh` | ✅ Организован |
| `create-vm.sh` (корень) | `infra/create-vm.sh` | ✅ Категоризирован |

---

## 🎯 Примеры использования

### Сценарий 1: Изменили код локально и хотят задеплоить

```bash
# 1. Тестируем локально
./scripts/local/start.sh

# 2. Проверяем что всё работает
open http://localhost:5173

# 3. Деплоим на продакшн
./scripts/prod/deploy.sh

# 4. Проверяем продакшн
curl http://46.21.244.23/health
```

### Сценарий 2: Быстрый фикс только в backend

```bash
# 1. Изменили файлы в backend/
# 2. Быстро обновляем только backend
./scripts/prod/update-backend.sh

# 3. Проверяем логи
ssh yc-user@46.21.244.23 'cd ~/medhistory && ./scripts/utils/logs.sh backend --tail=50'
```

### Сценарий 3: Что-то сломалось на продакшне

```bash
# 1. Диагностика
./scripts/prod/diagnose.sh

# 2. Смотрим логи в реальном времени
ssh yc-user@46.21.244.23 'cd ~/medhistory && ./scripts/utils/logs.sh -f'

# 3. Откат из бэкапа (если нужно)
ssh yc-user@46.21.244.23
cd ~/medhistory
./scripts/utils/restore.sh ~/backups/medhistory_backup_YYYYMMDD_HHMMSS.tar.gz
```

### Сценарий 4: Настройка нового сервера

```bash
# 1. Создать VM
./scripts/infra/create-vm.sh

# 2. Подключиться и настроить
ssh ubuntu@<NEW_IP>
# На сервере запустить setup (скопировать проект туда сначала)

# 3. Обновить config.sh с новым IP
nano scripts/config.sh

# 4. Первый деплой
./scripts/prod/deploy.sh
```

---

## 🔧 Переопределение параметров

Все продакшн скрипты поддерживают переопределение через аргументы:

```bash
# Использовать другой сервер
./scripts/prod/deploy.sh 10.20.30.40 ubuntu

# Обновить backend на тестовом сервере
./scripts/prod/update-backend.sh 192.168.1.100 testuser

# Диагностика на другом сервере
./scripts/prod/diagnose.sh staging.example.com deploy
```

---

## 🆘 Решение проблем

### Скрипт не выполняется
```bash
chmod +x scripts/**/*.sh
```

### Ошибка "config.sh not found"
```bash
# Проверьте, что config.sh существует
ls -la scripts/config.sh

# Если нет - он должен быть создан автоматически
```

### SSH подключение не работает
```bash
# Проверить доступ
ssh yc-user@46.21.244.23 'echo OK'

# Добавить ключ
ssh-copy-id yc-user@46.21.244.23
```

### Переменные окружения не подтягиваются
```bash
# Убедитесь что .env.production существует
ls -la .env.production

# Проверьте содержимое
cat .env.production | grep -v PASSWORD | grep -v SECRET
```

---

## 📚 Дополнительная документация

- [local/README.md](local/README.md) - Локальная разработка
- [../docs/change_log/](../docs/change_log/) - История изменений
- [../docs/yandex-cloud/](../docs/yandex-cloud/) - Документация по серверу

---

## 💡 Лучшие практики

1. **Всегда делайте бэкап перед обновлением**
   ```bash
   ssh yc-user@46.21.244.23 'cd ~/medhistory && ./scripts/utils/backup.sh ~/backups'
   ```

2. **Тестируйте локально перед деплоем**
   ```bash
   ./scripts/local/start.sh
   ```

3. **Используйте быстрые обновления для итераций**
   ```bash
   ./scripts/prod/update-backend.sh  # Вместо полного deploy
   ```

4. **Мониторьте логи после деплоя**
   ```bash
   ssh yc-user@46.21.244.23 'cd ~/medhistory && ./scripts/utils/logs.sh -f'
   ```

5. **Проверяйте health после изменений**
   ```bash
   curl http://46.21.244.23/health
   ```

---

## 🔄 Миграция со старой структуры

Старые скрипты **удалены и заменены** новыми. Обновите ваши команды:

| Старое | Новое |
|--------|-------|
| `./scripts/deploy.sh` | `./scripts/prod/deploy.sh` |
| `./scripts/update-backend.sh` | `./scripts/prod/update-backend.sh` |
| `./scripts/update-frontend.sh` | `./scripts/prod/update-frontend.sh` |
| `./scripts/backup.sh` | `./scripts/utils/backup.sh` |
| `./scripts/status.sh` | `./scripts/utils/status.sh` |
| `./scripts/diagnose-registration.sh` | `./scripts/prod/diagnose.sh` |

---

**Обновлено:** 28 октября 2025  
**Версия:** 2.0 (реорганизация структуры)
