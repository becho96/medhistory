# 🔧 Скрипты локальной разработки

Скрипты для управления локальным Docker окружением.

---

## 📋 Список скриптов

### 🚀 `start.sh` - Запуск приложения

Первый запуск или запуск после остановки.

**Использование:**
```bash
./scripts/local/start.sh
# или из корня:
./start.sh
```

**Что делает:**
- ✅ Проверяет наличие Docker и Docker Compose
- ✅ Создаёт `.env` файл из шаблона (если отсутствует)
- ✅ Предупреждает если не указан OpenRouter API ключ
- ✅ Запускает все контейнеры (postgres, mongodb, minio, backend, frontend)
- ✅ Показывает статус сервисов
- ✅ Выводит доступные URL

**Сервисы будут доступны на:**
- Frontend: http://localhost:5173
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs
- MinIO Console: http://localhost:9001

---

### ⏹️ `stop.sh` - Остановка приложения

Останавливает все контейнеры без удаления данных.

**Использование:**
```bash
./scripts/local/stop.sh
# или из корня:
./stop.sh
```

**Что делает:**
- ⏹️ Останавливает все контейнеры
- 💾 Сохраняет все данные в Docker volumes

---

### 🔄 `restart-local.sh` - Перезапуск с обновлением

**ОСНОВНОЙ СКРИПТ ДЛЯ РАЗРАБОТКИ**

Используйте после изменений в коде для применения изменений в Docker.

**Использование:**
```bash
# Обновить всё (backend + frontend)
./scripts/local/restart-local.sh

# Обновить только backend
./scripts/local/restart-local.sh backend

# Обновить только frontend
./scripts/local/restart-local.sh frontend

# Или короткие алиасы:
./scripts/local/restart-local.sh be    # backend
./scripts/local/restart-local.sh fe    # frontend
```

**Что делает:**
1. ⏹️ Останавливает указанные контейнеры
2. 🔨 Пересобирает Docker образы
3. ▶️ Запускает обновлённые контейнеры
4. 📝 Показывает логи в реальном времени

**Когда использовать:**
- ✅ После изменений в коде Python (backend)
- ✅ После изменений в коде React (frontend) - если hot reload не сработал
- ✅ После изменений в `requirements.txt` или `package.json`
- ✅ После изменений в `Dockerfile`
- ✅ После удаления/добавления файлов
- ✅ После изменений в конфигурации

---

### 🏭 `start-prod-local.sh` - Локальный запуск продакшн конфигурации

Запуск продакшн конфигурации локально для тестирования.

**Использование:**
```bash
./scripts/local/start-prod-local.sh
```

**Что делает:**
- Использует `docker-compose.prod.yml`
- Собирает оптимизированные production образы
- Использует Nginx как reverse proxy
- Тестирует production окружение локально

**Когда использовать:**
- Перед деплоем на продакшн
- Для тестирования production сборки
- Для отладки проблем, специфичных для production

---

## 🔍 Типичные сценарии

### Сценарий 1: Первый запуск проекта
```bash
cd "История болезни"
./scripts/local/start.sh
```

### Сценарий 2: Внесли изменения в код backend
```bash
# После редактирования файлов в backend/
./scripts/local/restart-local.sh backend

# Или из корня:
./restart.sh backend
```

### Сценарий 3: Удалили файлы или изменили структуру
```bash
# Например, удалили admin эндпоинты
./scripts/local/restart-local.sh
```

### Сценарий 4: Изменили зависимости
```bash
# После изменения requirements.txt или package.json
./scripts/local/restart-local.sh

# Или только нужный сервис:
./scripts/local/restart-local.sh backend  # если меняли requirements.txt
./scripts/local/restart-local.sh frontend # если меняли package.json
```

### Сценарий 5: Закончили работу
```bash
./scripts/local/stop.sh
```

### Сценарий 6: Полная пересборка (если что-то сломалось)
```bash
# Остановить и удалить контейнеры
docker compose down

# Пересобрать с нуля
docker compose build --no-cache

# Запустить
./scripts/local/start.sh
```

---

## 📝 Просмотр логов

```bash
# Все сервисы
docker compose logs -f

# Только backend
docker compose logs -f backend

# Только frontend
docker compose logs -f frontend

# Последние 50 строк backend
docker compose logs --tail=50 backend

# Поиск ошибок
docker compose logs backend | grep -i error
```

---

## 🐛 Решение проблем

### Контейнеры не запускаются
```bash
# Проверить статус
docker compose ps

# Посмотреть логи
docker compose logs

# Пересоздать контейнеры
docker compose up -d --force-recreate
```

### Порты заняты
```bash
# Найти процесс на порту
lsof -i :5173  # frontend
lsof -i :8000  # backend

# Убить процесс
kill -9 <PID>
```

### Нет свободного места
```bash
# Проверить размер Docker
docker system df

# Очистить неиспользуемое
docker system prune -a

# Удалить всё (ВНИМАНИЕ: удалит все Docker данные!)
docker system prune -a --volumes
```

### Backend не видит изменения в коде
```bash
# Python файлы кэшируются, нужна пересборка
./scripts/local/restart-local.sh backend
```

### Frontend не видит изменения
```bash
# Обычно hot reload работает, но если нет:
./scripts/local/restart-local.sh frontend

# Или очистить кэш браузера (Cmd+Shift+R)
```

---

## 💡 Полезные команды Docker

```bash
# Статус всех контейнеров
docker compose ps

# Статистика использования ресурсов
docker stats

# Зайти внутрь контейнера
docker exec -it medhistory_backend bash
docker exec -it medhistory_frontend sh

# Проверить сети
docker network ls
docker network inspect medhistory_medhistory_network

# Проверить volumes
docker volume ls
docker volume inspect medhistory_postgres_data

# Остановить всё
docker compose stop

# Удалить контейнеры (данные сохранятся)
docker compose down

# Удалить ВСЁ включая данные
docker compose down -v
```

---

## 🔗 Связанные документы

- [Основной README](../../README.md) - Общая документация проекта
- [Продакшн скрипты](../README.md) - Скрипты для развертывания на сервере
- [API документация](../../docs/API_DOCUMENTATION.md) - Документация API

---

**Обновлено:** 28 октября 2025

