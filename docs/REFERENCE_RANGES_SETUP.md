# Настройка референсных значений на графиках анализов

## Обзор

Данная функциональность добавляет отображение референсных (нормальных) значений анализов на графиках с учётом пола пациента. Система автоматически показывает границы нормы и окрашивает точки графика в зависимости от того, находятся ли значения в пределах нормы.

## Изменения в проекте

### Backend изменения:

1. **Добавлено поле `gender` в модель User**
   - Enum тип: `male`, `female`, `other`
   - Используется для определения референсных значений

2. **Обновлён API endpoint `/docs/labs/timeseries`**
   - Теперь возвращает `reference_min` и `reference_max`
   - Значения зависят от пола пользователя (profile_user_id)

3. **Добавлен endpoint PATCH `/auth/me`**
   - Позволяет обновлять профиль пользователя (имя, дата рождения, пол)

### Frontend изменения:

1. **Обновлён график на странице Анализов (`Labs.tsx`)**
   - Отображаются линии минимума и максимума референсных значений (красные пунктирные линии)
   - Точки окрашиваются в зависимости от статуса:
     - 🟢 Зелёный: значение в норме
     - 🟡 Жёлтый: близко к границе нормы (в пределах 10% от диапазона)
     - 🔴 Красный: вне нормы
   - Добавлена легенда, объясняющая значение линий и цветов

2. **Добавлен компонент настроек профиля (`ProfileSettings.tsx`)**
   - Кнопка "Профиль" в шапке приложения
   - Позволяет указать пол пользователя, имя, дату рождения

## Инструкция по применению

### Шаг 1: Применение миграции базы данных

Выполните миграцию для добавления поля `gender` в таблицу `users`:

```bash
cd backend

# Применение миграции (локально)
python scripts/apply_gender_migration.py

# Для production (с указанием URL БД)
python scripts/apply_gender_migration.py --database-url "postgresql://user:pass@host:port/dbname"
```

### Шаг 2: Проверка данных референсных значений

Убедитесь, что референсные значения загружены в базу данных:

```bash
cd backend

# Проверка наличия референсных значений
python scripts/populate_reference_ranges.py --dry-run

# Если данных нет, загрузите их
python scripts/populate_reference_ranges.py
```

### Шаг 3: Перезапуск backend

```bash
cd backend
# Перезапустите backend сервер для применения изменений в моделях
```

### Шаг 4: Перезапуск frontend

```bash
cd frontend
# Если используется dev-сервер, перезагрузите страницу в браузере
# Если production, пересоберите проект
npm run build
```

## Использование

### Для пользователей:

1. **Указание пола:**
   - Нажмите кнопку "Профиль" в правом верхнем углу
   - Выберите ваш пол (Мужской/Женский/Другой)
   - Нажмите "Сохранить"

2. **Просмотр графиков с референсными значениями:**
   - Перейдите на страницу "Анализы"
   - Выберите интересующий анализ
   - На графике отобразятся:
     - Красные пунктирные линии — границы нормы (минимум и максимум)
     - Зелёная пунктирная линия — среднее значение ваших анализов
     - Точки окрашены по статусу: зелёный (норма), жёлтый (близко к границе), красный (вне нормы)

3. **Легенда:**
   - Под графиком отображается легенда с расшифровкой всех элементов

## Технические детали

### Алгоритм определения статуса значения:

```
- Норма (зелёный): value >= reference_min AND value <= reference_max
- Близко к границе (жёлтый): 
  - value < reference_min AND value >= reference_min - 10% от диапазона
  - value > reference_max AND value <= reference_max + 10% от диапазона
- Вне нормы (красный): всё остальное
```

### Структура данных в БД:

**Таблица `analyte_standards`:**
- `reference_male_min` - минимальное значение для мужчин
- `reference_male_max` - максимальное значение для мужчин
- `reference_female_min` - минимальное значение для женщин
- `reference_female_max` - максимальное значение для женщин

**Таблица `users`:**
- `gender` - пол пользователя (enum: male, female, other)

### API Response структура:

```json
{
  "analyte": "Гемоглобин",
  "standard_unit": "г/л",
  "category": "Общий анализ крови",
  "reference_min": 120.0,
  "reference_max": 160.0,
  "points": [
    {
      "date": "2024-01-15",
      "value_num": 145.0,
      "unit": "г/л",
      "document_id": "...",
      "reference_range": "120-160",
      "flag": "N"
    }
  ]
}
```

## Поведение при отсутствии данных

- **Если пол не указан:** используются мужские референсные значения по умолчанию
- **Если референсные значения отсутствуют для анализа:** линии границ нормы не отображаются, точки остаются синими
- **Если один из пределов (min/max) отсутствует:** отображается только доступная граница

## Troubleshooting

### Линии референсных значений не отображаются

1. Проверьте, что пол указан в профиле пользователя
2. Убедитесь, что референсные значения загружены для данного анализа:
   ```sql
   SELECT canonical_name, reference_male_min, reference_male_max, 
          reference_female_min, reference_female_max
   FROM analyte_standards
   WHERE canonical_name = 'Название анализа';
   ```

### Ошибка при сохранении пола

Проверьте, что миграция применена:
```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'users' AND column_name = 'gender';
```

### Точки не окрашиваются в цвета

Проверьте консоль браузера на наличие ошибок. Убедитесь, что API возвращает `reference_min` и `reference_max`.

## Будущие улучшения

- Добавление возраста как фактора для референсных значений (детские нормы отличаются от взрослых)
- Персонализированные референсные значения для пользователей с хроническими заболеваниями
- История изменения референсных значений
- Экспорт графиков с референсными значениями в PDF
