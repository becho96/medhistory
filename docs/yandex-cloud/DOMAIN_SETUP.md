# 🌐 Настройка доменного имени для MedHistory

Полное руководство по покупке домена и настройке HTTPS для сервиса MedHistory.

---

## 📋 Содержание

1. [Выбор и покупка домена](#1-выбор-и-покупка-домена)
2. [Настройка DNS](#2-настройка-dns)
3. [Получение SSL сертификата](#3-получение-ssl-сертификата)
4. [Обновление конфигурации](#4-обновление-конфигурации)
5. [Обновление переменных окружения](#5-обновление-переменных-окружения)
6. [Проверка работы](#6-проверка-работы)

---

## 1. Выбор и покупка домена

### Рекомендуемые регистраторы

| Регистратор | Цена .ru/год | Ссылка | Особенности |
|-------------|--------------|--------|-------------|
| **reg.ru** | ~199₽ | [reg.ru](https://reg.ru) | Популярный, хорошая поддержка |
| **RU-CENTER** | ~249₽ | [nic.ru](https://nic.ru) | Официальный регистратор |
| **Beget** | ~199₽ | [beget.com](https://beget.com) | Удобный интерфейс |
| **Timeweb** | ~169₽ | [timeweb.ru](https://timeweb.ru) | Бюджетный вариант |

### Варианты доменных имён

Для медицинского сервиса подойдут:

- `medhistory.ru` — прямой и понятный
- `med-history.ru` — с дефисом  
- `mymedhistory.ru` — персональный
- `medcard.ru` — короткий
- `zdorovie.ru` — на русском

### Процесс покупки (на примере reg.ru)

1. Перейти на [reg.ru](https://reg.ru)
2. Ввести желаемое имя в поиске
3. Выбрать доступный вариант
4. Зарегистрироваться / войти в аккаунт
5. Заполнить данные владельца домена
6. Оплатить (от 199₽/год)

---

## 2. Настройка DNS

### Добавление A-записей

После покупки домена войдите в панель управления регистратора и добавьте следующие DNS-записи:

```
Тип:     A
Имя:     @              (или пустое — корневой домен)
Значение: 158.160.99.232
TTL:     3600

Тип:     A
Имя:     www
Значение: 158.160.99.232
TTL:     3600
```

### Пример настройки в разных панелях

#### reg.ru
1. Мои домены → Выбрать домен → DNS-серверы и управление зоной
2. Добавить запись → Тип: A
3. Заполнить значения как указано выше

#### RU-CENTER
1. Домены → Управление зоной
2. Добавить ресурсную запись → A
3. Заполнить значения

#### Beget
1. Домены → DNS → Управление DNS-записями
2. Добавить A-запись

### Проверка DNS

Проверить распространение DNS можно командой:

```bash
# macOS / Linux
dig +short ваш-домен.ru A

# или через онлайн-сервис
# https://dnschecker.org
```

⏱️ **Важно:** Распространение DNS может занять от 5 минут до 48 часов (обычно 15-30 минут).

---

## 3. Получение SSL сертификата

### Автоматическая настройка (рекомендуется)

Используйте подготовленный скрипт:

```bash
cd "/Users/boris/Desktop/Начало/История болезни"
./scripts/setup-domain.sh -d ваш-домен.ru -e admin@ваш-домен.ru
```

Скрипт автоматически:
- Проверит DNS-записи
- Получит SSL сертификат от Let's Encrypt
- Обновит конфигурацию nginx
- Настроит автоматическое обновление сертификата

### Ручная настройка

#### 3.1. Установка certbot на сервере

```bash
ssh yc-user@158.160.99.232

# Установка certbot
sudo apt update
sudo apt install -y certbot

# Остановка nginx для получения сертификата
cd ~/medhistory
docker compose stop nginx
```

#### 3.2. Получение сертификата

```bash
sudo certbot certonly --standalone \
    -d ваш-домен.ru \
    -d www.ваш-домен.ru \
    --email admin@ваш-домен.ru \
    --agree-tos
```

#### 3.3. Проверка сертификата

```bash
sudo ls -la /etc/letsencrypt/live/ваш-домен.ru/
```

Должны быть файлы:
- `fullchain.pem` — полная цепочка сертификатов
- `privkey.pem` — приватный ключ

---

## 4. Обновление конфигурации

### 4.1. Обновление nginx.conf

Замените содержимое `nginx/nginx.conf` на конфигурацию с SSL:

```nginx
# ... (см. полный пример в scripts/setup-domain.sh)

server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;
    
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name ваш-домен.ru www.ваш-домен.ru;
    
    ssl_certificate /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ваш-домен.ru/privkey.pem;
    
    # ... остальная конфигурация
}
```

### 4.2. Обновление docker-compose.yml

Добавьте монтирование сертификатов в сервис nginx:

```yaml
nginx:
  # ...
  volumes:
    - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    - /etc/letsencrypt:/etc/letsencrypt:ro  # добавить эту строку
```

### 4.3. Перезапуск сервисов

```bash
ssh yc-user@158.160.99.232 'cd ~/medhistory && docker compose up -d'
```

---

## 5. Обновление переменных окружения

### 5.1. Обновить .env.production на сервере

```bash
ssh yc-user@158.160.99.232 'nano ~/medhistory/.env.production'
```

Изменить:

```env
# API URL
VITE_API_URL=https://ваш-домен.ru/api

# Allowed origins (для CORS)
ALLOWED_ORIGINS=https://ваш-домен.ru,https://www.ваш-домен.ru
```

### 5.2. Обновить Google OAuth (если используется)

В [Google Cloud Console](https://console.cloud.google.com):
1. APIs & Services → Credentials
2. Выбрать OAuth 2.0 Client ID
3. Добавить Authorized redirect URIs:
   - `https://ваш-домен.ru/api/v1/auth/google/callback`

### 5.3. Пересобрать frontend

```bash
ssh yc-user@158.160.99.232 'cd ~/medhistory && docker compose build frontend && docker compose up -d frontend'
```

---

## 6. Проверка работы

### Проверка HTTPS

```bash
# Проверка сайта
curl -I https://ваш-домен.ru

# Проверка редиректа с HTTP
curl -I http://ваш-домен.ru
# Должен быть 301 редирект на https://

# Проверка API
curl https://ваш-домен.ru/api/v1/health
```

### Проверка SSL сертификата

```bash
# Через openssl
openssl s_client -connect ваш-домен.ru:443 -servername ваш-домен.ru < /dev/null 2>/dev/null | openssl x509 -noout -dates

# Или через онлайн-сервис
# https://www.ssllabs.com/ssltest/
```

### Доступные URL после настройки

| Сервис | URL |
|--------|-----|
| Фронтенд | `https://ваш-домен.ru` |
| API документация | `https://ваш-домен.ru/docs` |
| Grafana | `https://ваш-домен.ru/grafana/` |
| Health check | `https://ваш-домен.ru/health` |

---

## 🔄 Автоматическое обновление сертификата

Let's Encrypt сертификаты действительны 90 дней. Для автоматического обновления настройте cron:

```bash
ssh yc-user@158.160.99.232

# Добавить в crontab
crontab -e

# Добавить строку (обновление каждый день в 3:00)
0 3 * * * certbot renew --quiet --post-hook 'docker restart medhistory_nginx'
```

---

## 🔧 Troubleshooting

### DNS не распространяется

```bash
# Проверить на разных DNS серверах
dig @8.8.8.8 ваш-домен.ru A
dig @1.1.1.1 ваш-домен.ru A
```

### Ошибка получения сертификата

```bash
# Убедитесь что порт 80 открыт и nginx остановлен
sudo ss -tlnp | grep :80

# Попробуйте снова с verbose
sudo certbot certonly --standalone -d ваш-домен.ru -v
```

### Сертификат не обновляется

```bash
# Проверить статус сертификатов
sudo certbot certificates

# Принудительное обновление
sudo certbot renew --force-renewal
```

---

## 💰 Примерные расходы

| Статья | Стоимость | Период |
|--------|-----------|--------|
| Домен .ru | 199-299₽ | год |
| SSL сертификат (Let's Encrypt) | Бесплатно | — |
| Сервер Yandex Cloud | ~3,500₽ | месяц |
| **Итого** | ~3,700₽ | первый месяц |

---

**Дата создания:** 11 января 2026  
**Последнее обновление:** 11 января 2026
