# 🔄 Реорганизация скриптов управления

**Дата:** 28 октября 2025  
**Автор:** Борис  
**Версия:** 2.0

---

## 📝 Краткое описание

Полная реорганизация структуры скриптов управления проектом. Устранен хардкод, создана логичная иерархия, добавлен единый конфигурационный файл.

---

## 🎯 Проблемы, которые решили

### 1. **Хардкод в скриптах**
**Было:**
```bash
SERVER="yc-user@46.21.244.23"
PROJECT_PATH="/Users/boris/Desktop/Начало/История болезни"
```

**Стало:**
```bash
source scripts/config.sh
SERVER="${PROD_SERVER_USER}@${PROD_SERVER_IP}"
```

### 2. **Отсутствие организации**
- Все скрипты были в одной папке
- Непонятно какой скрипт для какой среды
- Нет разделения на категории

### 3. **Сложная навигация**
- Длинные имена скриптов
- Нужно помнить длинные пути
- Неудобно использовать часто

### 4. **Дублирование кода**
- Цвета и функции повторялись в каждом скрипте
- Настройки сервера дублировались

---

## ✨ Что изменилось

### Новая структура

```
scripts/
├── config.sh                 # 🆕 Единый конфигурационный файл
│
├── local/                    # ✅ Локальная разработка (было)
│   ├── start.sh
│   ├── stop.sh
│   ├── restart-local.sh
│   └── README.md
│
├── prod/                     # 🆕 Продакшн скрипты
│   ├── deploy.sh            # ✅ Без хардкода
│   ├── update-backend.sh    # ✅ Без хардкода
│   ├── update-frontend.sh   # ✅ Без хардкода
│   ├── update-all.sh        # ✅ Без хардкода
│   ├── diagnose.sh          # 🔄 Переименован из diagnose-registration.sh
│   └── README.md            # 🆕 Документация
│
├── infra/                    # 🆕 Инфраструктурные скрипты
│   ├── create-vm.sh         # 📦 Перемещен
│   └── setup-server.sh      # 📦 Перемещен
│
├── utils/                    # 🆕 Универсальные утилиты
│   ├── backup.sh            # 📦 Перемещен
│   ├── restore.sh           # 📦 Перемещен
│   ├── status.sh            # 📦 Перемещен
│   └── logs.sh              # 📦 Перемещен
│
├── README.md                 # ✅ Обновлен
└── MIGRATION_GUIDE.md        # 🆕 Гайд по миграции
```

### Алиасы в корне проекта

```
Корень проекта/
├── dev.sh                    # 🆕 Алиас для ./scripts/local/start.sh
├── stop.sh                   # 🆕 Алиас для ./scripts/local/stop.sh
├── restart.sh                # 🆕 Алиас для ./scripts/local/restart-local.sh
└── deploy.sh                 # 🆕 Алиас для ./scripts/prod/deploy.sh
```

---

## 🔧 Новый config.sh

Создан единый конфигурационный файл со всеми настройками:

```bash
# Продакшн сервер
export PROD_SERVER_IP="46.21.244.23"
export PROD_SERVER_USER="yc-user"

# Пути
export REMOTE_PROJECT_DIR="~/medhistory"
export REMOTE_BACKUP_DIR="~/backups"

# Цвета
export COLOR_GREEN='\033[0;32m'
export COLOR_YELLOW='\033[1;33m'
...

# Функции
print_success() { ... }
print_error() { ... }
```

Все скрипты теперь подключают этот файл:
```bash
source "${SCRIPT_DIR}/../config.sh"
```

---

## 📋 Что удалено

- ❌ `scripts/deploy.sh` (старая версия с хардкодом)
- ❌ `scripts/update-backend.sh` (старая версия с хардкодом)
- ❌ `scripts/update-frontend.sh` (старая версия с хардкодом)
- ❌ `scripts/update-all.sh` (старая версия с хардкодом)
- ❌ `scripts/diagnose-registration.sh` (переименован в diagnose.sh)

---

## 📋 Что перемещено

| Было | Стало |
|------|-------|
| `scripts/create-vm.sh` | `scripts/infra/create-vm.sh` |
| `scripts/setup-server.sh` | `scripts/infra/setup-server.sh` |
| `scripts/backup.sh` | `scripts/utils/backup.sh` |
| `scripts/restore.sh` | `scripts/utils/restore.sh` |
| `scripts/status.sh` | `scripts/utils/status.sh` |
| `scripts/logs.sh` | `scripts/utils/logs.sh` |

---

## 📋 Что создано

### Новые файлы
1. `scripts/config.sh` - единый конфигурационный файл
2. `scripts/prod/deploy.sh` - новая версия без хардкода
3. `scripts/prod/update-backend.sh` - новая версия без хардкода
4. `scripts/prod/update-frontend.sh` - новая версия без хардкода
5. `scripts/prod/update-all.sh` - новая версия без хардкода
6. `scripts/prod/diagnose.sh` - переименованная версия
7. `scripts/prod/README.md` - документация продакшн скриптов
8. `scripts/MIGRATION_GUIDE.md` - гайд по миграции
9. `dev.sh` - алиас для локального запуска
10. `stop.sh` - алиас для остановки
11. `restart.sh` - алиас для перезапуска
12. `deploy.sh` - алиас для деплоя

### Обновлены
- `scripts/README.md` - обновлена документация
- `scripts/local/README.md` - обновлены ссылки

---

## 💡 Преимущества новой структуры

### 1. Конфигурируемость
```bash
# Легко сменить сервер
export PROD_SERVER_IP="192.168.1.100"
./scripts/prod/deploy.sh

# Или в config.sh один раз
nano scripts/config.sh
```

### 2. Организация
- ✅ Понятно где локальные скрипты: `scripts/local/`
- ✅ Понятно где продакшн: `scripts/prod/`
- ✅ Понятно где инфраструктура: `scripts/infra/`
- ✅ Универсальные утилиты: `scripts/utils/`

### 3. Удобство
```bash
# Было
./scripts/local/start.sh

# Стало (короче)
./dev.sh
```

### 4. DRY (Don't Repeat Yourself)
- Цвета в одном месте
- Функции в одном месте
- Настройки в одном месте

### 5. Документация
- README в каждой папке
- MIGRATION_GUIDE для перехода
- Примеры использования

---

## 🔄 Миграция

### Для разработчиков

**До:**
```bash
./scripts/local/start.sh
./scripts/update-backend.sh  # хардкод внутри
```

**После:**
```bash
./dev.sh                              # короче
./scripts/prod/update-backend.sh     # без хардкода
```

### Для CI/CD (если есть)

Обновить пути в pipeline:
```yaml
# До
- ./scripts/deploy.sh

# После
- ./scripts/prod/deploy.sh
```

---

## ✅ Тестирование

Проверено:
- [x] Локальный запуск через `./dev.sh`
- [x] Все скрипты имеют права на выполнение
- [x] Config.sh загружается корректно
- [x] Продакшн скрипты используют config.sh
- [x] Алиасы работают
- [x] README файлы обновлены

---

## 📚 Документация

Обновлена документация:
1. `scripts/README.md` - главная документация
2. `scripts/prod/README.md` - продакшн скрипты
3. `scripts/MIGRATION_GUIDE.md` - гайд по миграции
4. Этот файл - `SCRIPTS_REORGANIZATION_20251028.md`

---

## 🔮 Дальнейшие улучшения

Запланировано:
- [ ] Добавить скрипты для staging окружения (`scripts/staging/`)
- [ ] CI/CD интеграция (GitHub Actions)
- [ ] Версионирование Docker образов
- [ ] Автотесты перед деплоем
- [ ] Blue-Green deployment
- [ ] Health checks после деплоя
- [ ] Slack/Telegram уведомления

---

## 📊 Статистика изменений

- **Файлов создано:** 12
- **Файлов перемещено:** 6
- **Файлов удалено:** 5
- **Строк кода:** ~2000 строк (включая документацию)
- **Устранено хардкода:** 4 скрипта
- **Новых папок:** 3 (prod/, infra/, utils/)

---

## 🎓 Уроки

### Что узнали
1. **Хардкод - зло.** Конфигурация должна быть отдельно.
2. **Организация важна.** Понятная структура = меньше ошибок.
3. **DRY принцип.** Переиспользование кода экономит время.
4. **Документация критична.** Без README никто не разберется.
5. **Алиасы удобны.** Короткие команды = быстрая работа.

### Best Practices
- ✅ Один конфигурационный файл
- ✅ Логичная структура папок
- ✅ Документация в каждой папке
- ✅ Алиасы для частых команд
- ✅ Гайд по миграции для команды

---

## 🙏 Благодарности

Спасибо Claude (Cursor) за помощь в реорганизации! 🤖

---

**Статус:** ✅ Завершено  
**Следующий шаг:** Протестировать в боевых условиях

