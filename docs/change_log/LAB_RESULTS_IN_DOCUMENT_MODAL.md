# Отображение результатов анализов в карточке документа

## Дата изменения
19 октября 2025

## Описание
Добавлено отображение извлеченных результатов анализов в табличном виде непосредственно в модальном окне деталей документа (DocumentDetailsModal). Теперь пользователю не нужно нажимать на отдельную иконку анализа - результаты отображаются автоматически, если документ имеет тип "Результаты анализа".

## Внесенные изменения

### 1. Создан новый компонент `LabResultsTable.tsx`
**Путь:** `frontend/src/components/Documents/LabResultsTable.tsx`

**Функционал:**
- Автоматически проверяет тип документа
- Загружает извлеченные результаты анализов через API
- Отображает результаты в табличном виде с колонками:
  - Аналит (название теста)
  - Значение
  - Единица измерения
  - Референсный диапазон
  - Флаг (H - выше нормы, L - ниже нормы, N - норма, A - аномальное)
- Показывает легенду для расшифровки флагов
- Обрабатывает состояния загрузки и отсутствия данных
- Отображается только для документов типа "Результаты анализа"

### 2. Интеграция в `DocumentDetailsModal.tsx`
**Путь:** `frontend/src/components/Database/DocumentDetailsModal.tsx`

**Изменения:**
- Добавлен импорт нового компонента `LabResultsTable`
- Компонент размещен после секции "Превью документа" и перед секцией "PostgreSQL данные"
- Передаются параметры: `documentId` и `documentType`

## Поведение

### Для документов типа "Результаты анализа":
1. При открытии карточки документа автоматически загружаются результаты анализов
2. Если анализы извлечены, они отображаются в таблице с цветовой индикацией флагов
3. Если анализы не извлечены, показывается предупреждение
4. Отображается легенда для расшифровки флагов

### Для остальных типов документов:
- Компонент не отображается

## Цветовая индикация флагов
- 🟢 **N (Норма)**: зеленый цвет - значение в пределах нормы
- 🔴 **H (High)**: красный цвет - значение выше нормы
- 🟡 **L (Low)**: желтый цвет - значение ниже нормы
- 🟣 **A (Abnormal)**: фиолетовый цвет - аномальное значение

## Преимущества
1. Улучшенный UX - пользователь видит результаты сразу при открытии карточки
2. Не требуется дополнительных кликов для просмотра анализов
3. Единое место для просмотра всей информации о документе
4. Сохранена возможность открывать анализы отдельно через иконку на странице "Документы"

## Технические детали
- Используются существующие API эндпоинты: `getLabsSummary()` и `getLabs()`
- Компонент полностью типизирован с TypeScript
- Обработка ошибок через toast-уведомления
- Адаптивный дизайн с overflow-x для длинных таблиц

