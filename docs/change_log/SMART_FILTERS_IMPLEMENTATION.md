# Реализация умной системы фильтрации документов

**Дата:** 19 октября 2025  
**Автор:** AI Assistant  
**Версия:** 1.0

---

## 📋 Обзор

Реализована интеллектуальная система фильтрации документов с автокомплитом, множественным выбором и адаптивными фильтрами, которые меняются в зависимости от выбранного типа документа.

---

## ✨ Основные возможности

### 1. **Автокомплит с множественным выбором**
- При клике на поле фильтра появляется выпадающий список со всеми доступными значениями
- Можно выбрать несколько значений одновременно
- Поиск по мере ввода - список автоматически фильтруется
- Выбранные значения отображаются как chips (теги) с возможностью удаления

### 2. **Адаптивные фильтры**
Фильтры динамически меняются в зависимости от выбранного типа документа:

- **Для "Прием врача"** → показывается фильтр "Специализация"
- **Для "Инструментальное исследование"** → показываются "Подтип документа" и "Область исследования"
- **Для "Результаты анализа" и "Функциональная диагностика"** → показывается "Подтип документа"
- **Для "Другое"** → специфичные фильтры не показываются

### 3. **Стандартные фильтры**
Всегда доступны:
- **Имя пациента** - с автокомплитом
- **Медицинское учреждение** - с автокомплитом
- **Дата документа** (от и до) - дата создания медицинского документа
- **Дата загрузки** (от и до) - дата загрузки в систему (в расширенных фильтрах)

### 4. **Визуальные улучшения**
- Выбранные фильтры отображаются как chips с крестиком для удаления
- Кнопка "Очистить все" для быстрого сброса множественного выбора
- Индикатор активных фильтров
- Подсказка о необходимости выбрать тип документа для доступа к адаптивным фильтрам

---

## 🏗️ Архитектура

### Backend (Python/FastAPI)

#### 1. **Новые методы в `DocumentService`**

```python
@staticmethod
async def get_distinct_mongodb_field_values(
    user_id: uuid.UUID,
    field: str,
    q: Optional[str] = None,
    limit: int = 50
) -> list[str]:
    """Получение уникальных значений для MongoDB полей"""
```

```python
@staticmethod
async def filter_documents_by_mongodb_fields(
    user_id: uuid.UUID,
    specialties: Optional[list[str]] = None,
    document_subtype: Optional[list[str]] = None,
    research_area: Optional[list[str]] = None,
) -> list[str]:
    """Фильтрация документов по MongoDB полям"""
```

#### 2. **Обновленный метод `get_documents`**

Теперь поддерживает:
- Множественный выбор для всех фильтров (массивы вместо строк)
- Фильтрацию по MongoDB полям (specialties, document_subtype, research_area)
- Фильтрацию по дате загрузки (created_from, created_to)

#### 3. **Новый endpoint `/documents/filters/values`**

```http
GET /api/v1/documents/filters/values?field=specialties&q=кардио&limit=50
```

**Параметры:**
- `field` (required) - название поля: `document_type`, `patient_name`, `medical_facility`, `specialties`, `document_subtype`, `research_area`
- `q` (optional) - поисковый запрос для фильтрации значений
- `limit` (optional) - максимальное количество возвращаемых значений (по умолчанию 50)

**Ответ:**
```json
{
  "field": "specialties",
  "values": ["Кардиология", "Кардиохирургия"]
}
```

### Frontend (React/TypeScript)

#### 1. **Новый компонент `MultiSelect`**

Расположение: `frontend/src/components/Documents/MultiSelect.tsx`

**Особенности:**
- Автокомплит с поиском
- Множественный выбор
- Chips для отображения выбранных значений
- Обработка клика вне компонента для закрытия
- Debounce для поискового запроса (300ms)

**Props:**
```typescript
interface MultiSelectProps {
  label: string
  placeholder?: string
  values: string[]
  selectedValues: string[]
  onChange: (selected: string[]) => void
  onSearch?: (query: string) => void
  loading?: boolean
  disabled?: boolean
}
```

#### 2. **Обновленный `DocumentFilters`**

**Новые возможности:**
- Адаптивная логика отображения фильтров
- Использование `MultiSelect` для всех фильтров с автокомплитом
- Автоматическая загрузка значений фильтров из API
- Динамическая загрузка адаптивных фильтров при изменении типа документа

**Структура фильтров:**
```typescript
interface DocumentFilterValues {
  document_type?: string[]
  specialties?: string[]
  document_subtype?: string[]
  research_area?: string[]
  medical_facility?: string[]
  patient_name?: string[]
  date_from?: string
  date_to?: string
  created_from?: string
  created_to?: string
}
```

#### 3. **Обновленный сервис `documentsService`**

Добавлен метод:
```typescript
async getFilterValues(
  field: string, 
  q?: string, 
  limit?: number
): Promise<string[]>
```

---

## 🎯 Логика работы адаптивных фильтров

### Правила отображения фильтров

```typescript
// 1. Специализация - только для "Прием врача"
if (selectedTypes.includes('Прием врача')) {
  // показать фильтр "Специализация"
}

// 2. Подтип документа - для анализов, инструментальных и функциональных исследований
if (
  selectedTypes.includes('Результаты анализа') ||
  selectedTypes.includes('Инструментальное исследование') ||
  selectedTypes.includes('Функциональная диагностика')
) {
  // показать фильтр "Подтип документа"
}

// 3. Область исследования - только для инструментальных исследований
if (selectedTypes.includes('Инструментальное исследование')) {
  // показать фильтр "Область исследования"
}
```

### Пример сценария использования

1. **Пользователь открывает страницу документов**
   - Видит основные фильтры: Имя пациента, Медучреждение, Дата документа

2. **Пользователь нажимает "Развернуть"**
   - Появляются дополнительные фильтры, включая "Тип документа"
   - Видит подсказку о необходимости выбрать тип документа

3. **Пользователь выбирает "Прием врача"**
   - Автоматически появляется фильтр "Специализация"
   - Система загружает доступные специализации из MongoDB

4. **Пользователь начинает вводить "кард"**
   - Список специализаций фильтруется: "Кардиология", "Кардиохирургия"
   - Пользователь выбирает "Кардиология"

5. **Пользователь выбирает дополнительно "Прием врача" и "Результаты анализа"**
   - Появляется фильтр "Подтип документа"
   - Загружаются подтипы для обоих типов документов

6. **Результат**
   - Система показывает только документы, соответствующие всем выбранным фильтрам
   - Фильтры отображаются как chips с возможностью быстрого удаления

---

## 📊 Технические детали

### Оптимизация производительности

1. **Debounce для поиска** - задержка 300ms перед отправкой запроса
2. **Lazy loading фильтров** - адаптивные фильтры загружаются только при выборе соответствующего типа документа
3. **Кэширование в React Query** - результаты запросов кэшируются на стороне клиента
4. **MongoDB aggregation** - эффективные запросы для получения уникальных значений

### Обработка edge cases

1. **Пустые значения** - фильтры с пустыми значениями не отправляются на сервер
2. **Несколько типов документов** - адаптивные фильтры объединяются (OR)
3. **Отсутствие данных** - показывается сообщение "Нет доступных значений"
4. **Ошибки загрузки** - обрабатываются с выводом в консоль

### Интеграция с MongoDB

Фильтрация по MongoDB полям происходит в два этапа:

1. **Этап 1: Фильтрация в MongoDB**
   ```python
   document_ids = await filter_documents_by_mongodb_fields(
       user_id, specialties, document_subtype, research_area
   )
   ```

2. **Этап 2: Фильтрация в PostgreSQL**
   ```python
   query = select(Document).where(Document.id.in_(document_ids))
   # + остальные PostgreSQL фильтры
   ```

Это обеспечивает эффективную фильтрацию с использованием преимуществ обеих баз данных.

---

## 🧪 Тестирование

### Ручное тестирование

1. **Базовые фильтры**
   - [ ] Фильтрация по имени пациента
   - [ ] Фильтрация по медучреждению
   - [ ] Фильтрация по датам документа
   - [ ] Фильтрация по датам загрузки

2. **Множественный выбор**
   - [ ] Выбор нескольких значений в одном фильтре
   - [ ] Отображение chips
   - [ ] Удаление отдельных chips
   - [ ] Кнопка "Очистить все"

3. **Адаптивные фильтры**
   - [ ] Появление "Специализация" при выборе "Прием врача"
   - [ ] Появление "Подтип документа" для соответствующих типов
   - [ ] Появление "Область исследования" для инструментальных исследований
   - [ ] Скрытие неактуальных фильтров при смене типа

4. **Автокомплит**
   - [ ] Открытие выпадающего списка при клике
   - [ ] Фильтрация списка при вводе
   - [ ] Выбор значения из списка
   - [ ] Закрытие списка при клике вне компонента

5. **Производительность**
   - [ ] Debounce работает корректно
   - [ ] Нет лишних запросов к API
   - [ ] UI отзывчивый при большом количестве документов

---

## 📝 Дополнительная информация

### Связь с классификацией документов

Система фильтрации тесно интегрирована с правилами классификации документов из `DOCUMENT_CLASSIFICATION_RULES.md`:

- **5 типов документов** соответствуют фиксированному списку в правилах
- **Специализации** хранятся как массив в MongoDB для документов типа "Прием врача"
- **Подтипы** используются для анализов, инструментальных и функциональных исследований
- **Область исследования** специфична для инструментальных исследований

### Будущие улучшения

1. **Сохранение фильтров** - сохранение выбранных фильтров в localStorage
2. **Предустановленные наборы** - быстрый выбор часто используемых комбинаций фильтров
3. **Умные подсказки** - предложение фильтров на основе истории использования
4. **Экспорт результатов** - экспорт отфильтрованных документов
5. **Расширенный поиск** - полнотекстовый поиск по содержимому документов

---

## 🔧 Поддержка и обслуживание

### Добавление нового фильтра

1. **Backend:**
   - Добавить поле в `get_distinct_mongodb_field_values` или `get_distinct_field_values`
   - Обновить метод `filter_documents_by_mongodb_fields` или `get_documents`

2. **Frontend:**
   - Добавить поле в интерфейс `DocumentFilterValues`
   - Добавить state для значений фильтра
   - Добавить компонент `MultiSelect` в `DocumentFilters`
   - Обновить логику адаптивности (если нужно)

### Отладка

1. **Backend:**
   - Логи MongoDB запросов
   - Проверка производительности aggregation pipeline
   - Валидация входных параметров

2. **Frontend:**
   - React DevTools для проверки state
   - Network tab для проверки API запросов
   - Console для ошибок

---

## 📖 Дополнительные ресурсы

- [DOCUMENT_CLASSIFICATION_RULES.md](../ТЗ/DOCUMENT_CLASSIFICATION_RULES.md) - правила классификации документов
- [medical-history-tdd.md](../ТЗ/medical-history-tdd.md) - техническое задание

---

## ✅ Чеклист завершения

- [x] Backend API endpoints реализованы
- [x] MongoDB queries оптимизированы
- [x] Frontend компонент MultiSelect создан
- [x] DocumentFilters обновлен с адаптивной логикой
- [x] Интеграция с Documents.tsx
- [x] Типы TypeScript обновлены
- [x] Фильтры по дате создания и загрузки добавлены
- [x] Документация создана

---

**Статус:** ✅ Готово к использованию  
**Версия:** 1.0  
**Дата завершения:** 19 октября 2025

