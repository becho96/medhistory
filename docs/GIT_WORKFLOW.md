# 🌳 Git Workflow для MedHistory

Стандартизированный процесс работы с Git для команды разработки MedHistory.

## 📋 Содержание

1. [Обзор](#обзор)
2. [Структура веток](#структура-веток)
3. [Жизненный цикл фичи](#жизненный-цикл-фичи)
4. [Соглашения по именованию](#соглашения-по-именованию)
5. [Commit Messages](#commit-messages)
6. [Pull Requests](#pull-requests)
7. [Release Process](#release-process)

---

## 🎯 Обзор

Мы используем **Git Flow** - проверенную модель ветвления для управления релизами.

### Преимущества

✅ Четкое разделение production и development кода  
✅ Параллельная разработка нескольких фич  
✅ Простой откат на предыдущие версии  
✅ Контролируемые релизы  
✅ Легко понять что где происходит

---

## 🌿 Структура веток

```
main (production) ──────●────────●─────────●──────→
                        ↑        ↑         ↑
                        │        │         │
develop (staging) ──●───┴────●───┴─────●───┴───●───→
                    ↑        ↑         ↑       ↑
                    │        │         │       │
feature/user-profile┴───●────┘         │       │
                                       │       │
feature/ai-analysis────────────────────┴───●───┘
                                           
hotfix/security-fix ──────────────────────────●───→
```

### 📌 main

**Назначение:** Production ветка  
**Защита:** Прямые коммиты запрещены  
**Merge из:** `develop`, `hotfix/*`  
**Deploy:** Автоматический на production

**Правила:**
- Всегда стабильная, рабочая версия
- Каждый коммит = релиз
- Merge только через Pull Request
- Обязательно тегировать версии

### 📌 develop

**Назначение:** Интеграционная ветка  
**Защита:** Прямые коммиты не рекомендуются  
**Merge из:** `feature/*`, `bugfix/*`, `hotfix/*`  
**Deploy:** Автоматический на staging

**Правила:**
- Всегда готова к мержу в main
- Содержит последние завершенные фичи
- Регулярно тестируется на staging
- База для новых feature веток

### 📌 feature/*

**Назначение:** Разработка новой функциональности  
**Создается из:** `develop`  
**Merge в:** `develop`  
**Удаляется после:** Успешного merge

**Примеры:**
- `feature/user-authentication`
- `feature/document-upload`
- `feature/ai-interpretation`

### 📌 bugfix/*

**Назначение:** Исправление багов в develop  
**Создается из:** `develop`  
**Merge в:** `develop`  
**Удаляется после:** Успешного merge

**Примеры:**
- `bugfix/login-error`
- `bugfix/file-upload-timeout`

### 📌 hotfix/*

**Назначение:** Срочные исправления в production  
**Создается из:** `main`  
**Merge в:** `main` И `develop`  
**Удаляется после:** Успешного merge

**Примеры:**
- `hotfix/security-vulnerability`
- `hotfix/critical-crash`

---

## 🔄 Жизненный цикл фичи

### Этап 1: Создание ветки

```bash
# Обновляем develop
git checkout develop
git pull origin develop

# Создаем feature ветку
git checkout -b feature/add-user-profile

# Проверяем что мы на правильной ветке
git branch
```

### Этап 2: Разработка

```bash
# Запускаем локальное окружение
./deploy local

# Разрабатываем функциональность
# ... редактируем файлы ...

# Проверяем изменения
git status
git diff

# Добавляем изменения
git add .

# Коммитим (см. раздел Commit Messages)
git commit -m "feat: добавлена страница профиля пользователя"

# Пушим в remote
git push origin feature/add-user-profile
```

### Этап 3: Промежуточные коммиты

```bash
# Во время разработки делайте атомарные коммиты
git add frontend/src/pages/UserProfile.tsx
git commit -m "feat: создан компонент профиля пользователя"

git add backend/app/api/v1/endpoints/profile.py
git commit -m "feat: добавлен API endpoint для профиля"

git add frontend/src/services/profileService.ts
git commit -m "feat: добавлен сервис для работы с профилем"

# Пушим все коммиты
git push origin feature/add-user-profile
```

### Этап 4: Синхронизация с develop

```bash
# Если develop обновился пока вы работали
git checkout develop
git pull origin develop

git checkout feature/add-user-profile
git rebase develop

# Или через merge (если уже есть публичные коммиты)
git merge develop

# Пушим обновления
git push origin feature/add-user-profile --force-with-lease
```

### Этап 5: Тестирование на Staging

```bash
# Переключаемся на develop
git checkout develop

# Временно мержим feature для тестирования
git merge feature/add-user-profile --no-commit --no-ff

# Тестируем
./deploy staging

# Если всё ОК - завершаем merge
git commit -m "Merge feature/add-user-profile for testing"

# Если НЕ ОК - откатываем
git merge --abort

# Возвращаемся на feature ветку и исправляем
git checkout feature/add-user-profile
```

### Этап 6: Pull Request

```bash
# На GitHub/GitLab создаем PR:
# feature/add-user-profile → develop

# В описании PR указываем:
# - Что реализовано
# - Как тестировать
# - Скриншоты (если UI)
# - Связанные issue
```

### Этап 7: Code Review

```bash
# После получения комментариев
git checkout feature/add-user-profile

# Вносим правки
# ... правим код ...

git add .
git commit -m "fix: исправлены замечания code review"
git push origin feature/add-user-profile

# PR автоматически обновится
```

### Этап 8: Merge и очистка

```bash
# После одобрения PR
git checkout develop
git merge feature/add-user-profile
git push origin develop

# Удаляем локальную ветку
git branch -d feature/add-user-profile

# Удаляем удаленную ветку
git push origin --delete feature/add-user-profile
```

---

## 📛 Соглашения по именованию

### Ветки

**Формат:** `<тип>/<краткое-описание>`

**Типы:**
- `feature/` - новая функциональность
- `bugfix/` - исправление бага
- `hotfix/` - критическое исправление production
- `refactor/` - рефакторинг кода
- `docs/` - изменения в документации
- `test/` - добавление тестов

**Примеры:**

✅ Хорошо:
```
feature/user-authentication
bugfix/login-redirect
hotfix/security-patch
refactor/api-structure
docs/deployment-guide
test/user-service
```

❌ Плохо:
```
my-branch
fix
feature
user-profile-that-allows-users-to-edit-their-personal-information
```

### Правила именования

1. **Используйте kebab-case** (слова через дефис)
2. **Будьте конкретны** но лаконичны
3. **Используйте английский** для веток и коммитов
4. **Не используйте личные префиксы** (boris/feature, ...)
5. **Длина:** 3-5 слов максимум

---

## 💬 Commit Messages

### Формат

```
<тип>: <описание>

[необязательное тело]

[необязательный footer]
```

### Типы коммитов

| Тип | Описание | Пример |
|-----|----------|--------|
| `feat` | Новая функциональность | `feat: добавлена авторизация через OAuth` |
| `fix` | Исправление бага | `fix: исправлена ошибка загрузки файлов` |
| `docs` | Изменения в документации | `docs: обновлено руководство по деплою` |
| `style` | Форматирование кода | `style: применен prettier к компонентам` |
| `refactor` | Рефакторинг | `refactor: упрощена структура API endpoints` |
| `test` | Добавление тестов | `test: добавлены тесты для userService` |
| `chore` | Настройка инфраструктуры | `chore: обновлены зависимости` |
| `perf` | Улучшение производительности | `perf: оптимизирован запрос к БД` |

### Примеры

#### Простой коммит

```bash
git commit -m "feat: добавлен профиль пользователя"
```

#### Коммит с описанием

```bash
git commit -m "feat: добавлен профиль пользователя

Реализован полный функционал профиля:
- Просмотр личных данных
- Редактирование информации
- Загрузка аватара
- История активности"
```

#### Коммит с footer

```bash
git commit -m "fix: исправлена утечка памяти в AI сервисе

Проблема возникала при обработке больших файлов.
Добавлена очистка временных данных после обработки.

Closes #123"
```

### Правила commit messages

✅ **DO:**
- Используйте повелительное наклонение: "добавь", "исправь", "обнови"
- Начинайте с маленькой буквы (после типа)
- Будьте конкретны: что именно изменили
- Ссылайтесь на issue: `Closes #123`, `Refs #456`
- Пишите на русском (тип на английском)

❌ **DON'T:**
- Не используйте точку в конце
- Не пишите "изменения", "обновления" без конкретики
- Не делайте слишком длинные заголовки (>72 символов)
- Не группируйте несвязанные изменения в один коммит

### Шаблон commit message

Создайте файл `.gitmessage`:

```bash
# <тип>: <описание> (не более 72 символов)
# |<----  Используйте не более 72 символов   ---->|

# Объясните "что" и "почему", а не "как"
# |<----           Переносите на 72 символа              ---->|

# Укажите связанные issue
# Closes #123
# Refs #456

# --- ТИПЫ КОММИТОВ ---
# feat:     Новая функциональность
# fix:      Исправление бага
# docs:     Изменения в документации
# style:    Форматирование кода
# refactor: Рефакторинг без изменения функциональности
# test:     Добавление или изменение тестов
# chore:    Настройка инфраструктуры, обновление зависимостей
# perf:     Улучшение производительности
```

Активируйте шаблон:

```bash
git config --local commit.template .gitmessage
```

---

## 🔀 Pull Requests

### Создание PR

1. **Убедитесь что ветка обновлена:**

```bash
git checkout develop
git pull origin develop
git checkout feature/your-feature
git rebase develop
git push origin feature/your-feature --force-with-lease
```

2. **Создайте PR на GitHub/GitLab**

3. **Заполните описание по шаблону:**

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Новая функциональность
- [ ] Исправление бага
- [ ] Критическое исправление
- [ ] Рефакторинг
- [ ] Документация
- [ ] Тесты

## Как тестировать
1. Запустите `./deploy staging`
2. Откройте http://localhost:8080
3. Перейдите в раздел "Профиль"
4. Попробуйте редактировать данные

## Скриншоты
(если применимо)

## Чеклист
- [ ] Код прошел линтер
- [ ] Добавлены/обновлены тесты
- [ ] Документация обновлена
- [ ] Проверено на staging
- [ ] Нет конфликтов с develop

## Связанные issue
Closes #123
Refs #456
```

### Code Review чеклист

**Для автора PR:**
- [ ] Код проходит линтер без ошибок
- [ ] Все тесты проходят
- [ ] Нет закомментированного кода
- [ ] Нет console.log / print для отладки
- [ ] Документация обновлена
- [ ] Тестировано на staging

**Для ревьювера:**
- [ ] Код соответствует стилю проекта
- [ ] Нет дублирования кода
- [ ] Правильная обработка ошибок
- [ ] Безопасность (нет SQL injection, XSS и т.д.)
- [ ] Производительность (нет N+1 запросов, и т.д.)
- [ ] Читаемость и поддерживаемость

### Процесс Review

```bash
# Получаем PR локально для тестирования
git fetch origin pull/123/head:pr-123
git checkout pr-123

# Тестируем
./deploy local

# Оставляем комментарии на GitHub/GitLab
# После одобрения - жмем "Approve" и "Merge"
```

---

## 🚀 Release Process

### 1. Подготовка к релизу

```bash
# Убедитесь что develop стабилен
git checkout develop
git pull origin develop

# Тестируем на staging
./deploy staging

# Если есть баги - исправляем в develop
```

### 2. Создание Release ветки (опционально)

```bash
# Для больших релизов
git checkout develop
git checkout -b release/1.2.0

# Обновляем версию в package.json и других файлах
# Делаем финальные правки

git commit -m "chore: подготовка релиза 1.2.0"
git push origin release/1.2.0
```

### 3. Merge в main

```bash
# Создаем PR: develop (или release/1.2.0) → main
# После одобрения

git checkout main
git pull origin main
git merge develop --no-ff -m "Release v1.2.0"
```

### 4. Тегирование

```bash
# Создаем аннотированный тег
git tag -a v1.2.0 -m "Release v1.2.0

Новые функции:
- Профиль пользователя
- AI интерпретация результатов
- Улучшенный дашборд

Исправления:
- Исправлена загрузка больших файлов
- Улучшена производительность запросов"

# Пушим тег
git push origin v1.2.0

# Пушим main
git push origin main
```

### 5. Деплой на Production

```bash
# Деплой на production
./deploy production --monitoring

# Мониторим первые 15 минут
ssh yc-user@46.21.244.23 'cd ~/medhistory && docker compose -f docker-compose.base.yml -f docker-compose.prod.yml logs -f'
```

### 6. Синхронизация develop

```bash
# Если были hotfix в release ветке
git checkout develop
git merge main --no-ff
git push origin develop

# Удаляем release ветку
git branch -d release/1.2.0
git push origin --delete release/1.2.0
```

### 7. Release Notes

Создайте Release на GitHub/GitLab с описанием:

```markdown
# Release v1.2.0

## 🎉 Новые функции

- **Профиль пользователя:** Полный функционал редактирования профиля
- **AI интерпретация:** Автоматический анализ медицинских документов
- **Дашборд:** Новый дизайн с улучшенной навигацией

## 🐛 Исправления

- Исправлена загрузка файлов >10MB
- Улучшена производительность запросов к БД
- Исправлены ошибки в мобильной версии

## 📝 Изменения

- Обновлены зависимости
- Улучшена документация
- Добавлены тесты

## ⚠️ Breaking Changes

Нет

## 📦 Установка

```bash
git checkout v1.2.0
./deploy production
```

## 🙏 Благодарности

Спасибо всем участникам релиза!
```

---

## 🔥 Hotfix Process

### Когда использовать Hotfix

- 🚨 Критический баг в production
- 🔒 Уязвимость безопасности
- 💥 Падение сервиса
- 📉 Критическое снижение производительности

### Процесс

#### 1. Создание hotfix ветки

```bash
# Создаем из main
git checkout main
git pull origin main
git checkout -b hotfix/security-vulnerability
```

#### 2. Исправление

```bash
# Исправляем проблему
# ... редактируем код ...

git add .
git commit -m "fix: исправлена критическая уязвимость безопасности"
git push origin hotfix/security-vulnerability
```

#### 3. Тестирование

```bash
# Быстрое тестирование
./deploy staging

# Убедитесь что проблема решена
```

#### 4. Merge в main

```bash
# Создаем PR: hotfix/security-vulnerability → main
# Требуется быстрое одобрение

git checkout main
git merge hotfix/security-vulnerability --no-ff
```

#### 5. Тегирование

```bash
# Создаем patch версию
git tag -a v1.2.1 -m "Hotfix v1.2.1: Security vulnerability"
git push origin v1.2.1
git push origin main
```

#### 6. Деплой

```bash
# Немедленный деплой
./deploy production
```

#### 7. Merge в develop

```bash
# Синхронизируем develop
git checkout develop
git merge main --no-ff
git push origin develop

# Удаляем hotfix ветку
git branch -d hotfix/security-vulnerability
git push origin --delete hotfix/security-vulnerability
```

---

## 📊 Git команды - Шпаргалка

### Базовые команды

```bash
# Статус
git status

# История
git log --oneline --graph --all --decorate

# Создание ветки
git checkout -b feature/название

# Переключение между ветками
git checkout develop

# Обновление ветки
git pull origin develop

# Коммит
git add .
git commit -m "тип: описание"

# Push
git push origin название-ветки
```

### Работа с ветками

```bash
# Список веток
git branch -a

# Удаление локальной ветки
git branch -d название-ветки

# Удаление удаленной ветки
git push origin --delete название-ветки

# Переименование ветки
git branch -m старое-название новое-название
```

### Синхронизация

```bash
# Обновить все ветки
git fetch --all

# Rebase на develop
git checkout feature/название
git rebase develop

# Merge develop в feature
git checkout feature/название
git merge develop
```

### Откат изменений

```bash
# Откат не закоммиченных изменений
git checkout -- файл.txt
git checkout .

# Откат последнего коммита (сохранить изменения)
git reset --soft HEAD~1

# Откат последнего коммита (удалить изменения)
git reset --hard HEAD~1

# Откат до конкретного коммита
git reset --hard <commit-hash>
```

### Полезные алиасы

Добавьте в `~/.gitconfig`:

```ini
[alias]
    st = status
    co = checkout
    br = branch
    cm = commit -m
    lg = log --oneline --graph --all --decorate
    last = log -1 HEAD
    unstage = reset HEAD --
    
    # Показать изменения в последнем коммите
    show-last = show HEAD
    
    # Удалить все merged ветки
    cleanup = !git branch --merged | grep -v '\\*\\|main\\|develop' | xargs -n 1 git branch -d
```

Использование:

```bash
git st          # вместо git status
git co develop  # вместо git checkout develop
git cm "feat: новая функция"  # вместо git commit -m
git lg          # красивая история
git cleanup     # удалить merged ветки
```

---

## ✅ Чеклист для команды

### Ежедневно

- [ ] Pull последних изменений из develop
- [ ] Делайте атомарные коммиты
- [ ] Пушьте изменения в конце дня

### Перед созданием PR

- [ ] Rebase на develop
- [ ] Запустите линтер
- [ ] Проверьте на staging
- [ ] Заполните описание PR
- [ ] Добавьте ревьюверов

### Перед merge в develop

- [ ] Получено одобрение
- [ ] Нет конфликтов
- [ ] CI/CD пайплайн зеленый
- [ ] Тестировано на staging

### Перед релизом

- [ ] Все фичи протестированы
- [ ] Документация обновлена
- [ ] CHANGELOG обновлен
- [ ] Версия обновлена
- [ ] Release notes подготовлены

---

## 🎓 Best Practices

### ✅ DO

- ✅ Делайте маленькие, частые коммиты
- ✅ Пишите понятные commit messages
- ✅ Используйте feature ветки
- ✅ Регулярно синхронизируйтесь с develop
- ✅ Делайте code review
- ✅ Тестируйте перед PR
- ✅ Используйте rebase для чистой истории
- ✅ Удаляйте merged ветки

### ❌ DON'T

- ❌ Не коммитьте напрямую в main
- ❌ Не пушьте закомментированный код
- ❌ Не делайте гигантские коммиты
- ❌ Не используйте force push на публичных ветках
- ❌ Не оставляйте ветки без merge надолго
- ❌ Не игнорируйте конфликты
- ❌ Не merge без code review
- ❌ Не коммитьте .env файлы

---

## 🆘 Помощь

### Конфликты при merge

```bash
# При появлении конфликта
git status  # покажет конфликтующие файлы

# Откройте файлы и найдите
<<<<<<< HEAD
ваш код
=======
их код
>>>>>>> develop

# Решите конфликт, удалив маркеры
# Оставьте нужный код

git add .
git commit -m "fix: разрешены конфликты с develop"
```

### Случайно закоммитили в develop

```bash
# Создайте feature ветку с текущим состоянием
git branch feature/название

# Откатите develop
git reset --hard origin/develop

# Переключитесь на feature
git checkout feature/название
```

### Нужно изменить последний commit

```bash
# Внесите изменения
# ... редактируем файлы ...

git add .
git commit --amend --no-edit  # без изменения сообщения
# или
git commit --amend  # с изменением сообщения
```

---

## 📚 Ресурсы

- [Git Flow](https://nvie.com/posts/a-successful-git-branching-model/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Git Book](https://git-scm.com/book/ru/v2)
- [GitHub Flow](https://docs.github.com/en/get-started/quickstart/github-flow)

---

Следуя этому workflow, команда будет работать эффективно и организованно! 🚀

