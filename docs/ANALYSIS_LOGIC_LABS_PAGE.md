# Логика формирования информации на экране "Анализы"

## Обзор

Этот документ описывает полную логику формирования и отображения информации на странице "Анализы" (`/labs`) при выборе того или иного наименования анализа.

---

## 📊 Архитектура данных

### Хранение данных

**MongoDB** (`document_metadata` коллекция):
```javascript
{
  "document_id": "uuid",
  "user_id": "uuid",
  "extracted_data": {
    "lab_results": [
      {
        "test_name": "Гемоглобин",
        "value": "145",
        "unit": "г/л",
        "reference_range": "120-160",
        "flag": "N"  // N - норма, H - выше нормы, L - ниже нормы, A - аномальное
      }
    ]
  }
}
```

**PostgreSQL** (`documents` таблица):
- `id` (UUID)
- `document_date` (дата документа)
- `user_id` (UUID пользователя)
- Другие метаданные документа

---

## 🔄 Поток данных при выборе анализа

### 1. Загрузка списка доступных анализов

**Фронтенд:** `frontend/src/pages/Labs.tsx` (строки 491-494)

```typescript
const { data: analytesData } = useQuery({
  queryKey: ['labs_analytes'],
  queryFn: () => documentsService.listAnalytes(),
})
```

**Бэкенд:** `backend/app/api/v1/endpoints/documents.py` (строки 409-427)

**Эндпоинт:** `GET /api/v1/documents/labs/analytes`

**Запрос к MongoDB:**
```python
pipeline = [
    {"$match": {"user_id": str(current_user.id)}},
    {"$project": {"extracted_data.lab_results": 1}},
    {"$unwind": "$extracted_data.lab_results"},
    {"$group": {
        "_id": {"$toLower": "$extracted_data.lab_results.test_name"},
        "name": {"$first": "$extracted_data.lab_results.test_name"},
        "count": {"$sum": 1}
    }},
    {"$sort": {"name": 1}},
]
```

**Что делает:**
1. Фильтрует документы по `user_id`
2. Разворачивает массив `lab_results` (unwind)
3. Группирует по названию анализа (case-insensitive)
4. Считает количество измерений для каждого анализа
5. Сортирует по алфавиту

**Ответ:**
```json
{
  "analytes": [
    {"name": "Гемоглобин", "count": 5},
    {"name": "Лейкоциты", "count": 3}
  ]
}
```

---

### 2. Загрузка временного ряда для выбранного анализа

**Фронтенд:** `frontend/src/pages/Labs.tsx` (строки 496-500)

```typescript
const { data: seriesData, isLoading: loadingSeries } = useQuery({
  queryKey: ['labs_series', selected],
  queryFn: () => (selected 
    ? documentsService.getLabTimeSeries(selected) 
    : Promise.resolve({ analyte: selected, points: [] })
  ),
  enabled: !!selected,
})
```

**Бэкенд:** `backend/app/api/v1/endpoints/documents.py` (строки 430-494)

**Эндпоинт:** `GET /api/v1/documents/labs/timeseries?analyte=Гемоглобин`

**Шаг 1: Запрос к MongoDB**

```python
pipeline = [
    {"$match": {"user_id": str(current_user.id)}},
    {"$project": {"document_id": 1, "extracted_data.lab_results": 1}},
    {"$unwind": "$extracted_data.lab_results"},
    {"$match": {
        "extracted_data.lab_results.test_name": {
            "$regex": f"^{analyte}$", 
            "$options": "i"  # case-insensitive
        }
    }},
]
```

**Что делает:**
1. Фильтрует по `user_id`
2. Выбирает только `document_id` и `lab_results`
3. Разворачивает массив `lab_results`
4. Фильтрует по названию анализа (regex, case-insensitive)

**Результат:** Массив точек с данными:
```python
points = [
    {
        "document_id": "uuid1",
        "value": "145",
        "unit": "г/л",
        "reference_range": "120-160",
        "flag": "N"
    },
    ...
]
```

**Шаг 2: Обогащение датами из PostgreSQL**

```python
# Собираем все document_id
doc_ids = set([p["document_id"] for p in points])

# Запрос к PostgreSQL
q = select(DocumentModel.id, DocumentModel.document_date).where(
    DocumentModel.id.in_([UUID(x) for x in doc_ids])
)
result = await db.execute(q)
id_to_date = {str(r[0]): r[1] for r in result.all()}

# Добавляем даты к точкам
for p in points:
    p["date"] = id_to_date.get(p["document_id"])  # может быть None
```

**Шаг 3: Преобразование значений в числа**

```python
def try_float(v):
    try:
        return float(str(v).replace(',', '.'))  # Заменяет запятую на точку
    except Exception:
        return None

series = [
    {
        **p,
        "value_num": try_float(p.get("value")),
    }
    for p in points
]

# Фильтруем только те, у которых есть числовое значение
series = [p for p in series if p["value_num"] is not None]
```

**Шаг 4: Сортировка по дате**

```python
series.sort(key=lambda x: (x.get("date") is None, x.get("date") or ""))
```

**Финальный ответ:**
```json
{
  "analyte": "Гемоглобин",
  "points": [
    {
      "date": "2024-01-15",
      "value": "142",
      "value_num": 142.0,
      "unit": "г/л",
      "reference_range": "120-160",
      "flag": "N",
      "document_id": "uuid1"
    },
    {
      "date": "2024-03-20",
      "value": "145",
      "value_num": 145.0,
      "unit": "г/л",
      "reference_range": "120-160",
      "flag": "N",
      "document_id": "uuid2"
    }
  ]
}
```

---

## 🎨 Обработка данных на фронтенде

### Компонент `Labs.tsx`

**Файл:** `frontend/src/pages/Labs.tsx`

**Автоматический выбор первого анализа:**
```typescript
useEffect(() => {
  if (!selected && analytes.length > 0) {
    setSelected(analytes[0].name)
  }
}, [analytes, selected])
```

### Компонент `LineChart` (строки 12-486)

**Функция:** Визуализация временного ряда и расчет статистики

#### Расчет статистики (строки 26-35)

```typescript
const values = data.map((p) => p.value_num)
const minValue = Math.min(...values)
const maxValue = Math.max(...values)
const avgValue = values.reduce((a, b) => a + b, 0) / values.length

// Расчет тренда
const firstValue = values[0]
const lastValue = values[values.length - 1]
const trend = lastValue > firstValue ? 'up' : lastValue < firstValue ? 'down' : 'stable'
const trendPercent = firstValue !== 0 
  ? (((lastValue - firstValue) / Math.abs(firstValue)) * 100).toFixed(1) 
  : '0'
```

**Что рассчитывается:**
1. **Минимум** - минимальное значение из всех точек
2. **Максимум** - максимальное значение из всех точек
3. **Среднее** - среднее арифметическое всех значений
4. **Тренд** - направление изменения (рост/снижение/стабильно)
5. **Процент изменения** - процентное изменение от первого к последнему значению

#### Дата минимума/максимума (строки 96-108)

```typescript
{data.find(p => p.value_num === minValue)?.date && 
  new Date(data.find(p => p.value_num === minValue)!.date!).toLocaleDateString('ru-RU')}
```

**Что делает:** Находит точку с минимальным/максимальным значением и форматирует её дату.

#### Расчет отклонений от среднего (строки 445-475)

```typescript
const deviation = ((p.value_num - avgValue) / avgValue * 100).toFixed(1)
const isAboveAvg = p.value_num > avgValue
```

**Что рассчитывается:**
- Процент отклонения каждого значения от среднего
- Определение, выше или ниже среднего значение

#### Форматирование данных для графика

**Фильтрация и сортировка (строки 18-20):**
```typescript
const data = points
  .filter((p) => typeof p.value_num === 'number' && p.date)
  .sort((a, b) => new Date(a.date || '').getTime() - new Date(b.date || '').getTime())
```

**Расчет масштабов (строки 37-44):**
```typescript
const dates = data.map((p) => new Date(p.date!).getTime())
const minX = Math.min(...dates)
const maxX = Math.max(...dates)

// Добавление отступов для Y-оси
const yPadding = (maxValue - minValue) * 0.15 || 1
const minY = minValue - yPadding
const maxY = maxValue + yPadding
```

**Функции масштабирования (строки 49-57):**
```typescript
const xScale = (t: number) => {
  if (maxX === minX) return padding.left + chartWidth / 2
  return padding.left + ((t - minX) / (maxX - minX)) * chartWidth
}

const yScale = (v: number) => {
  if (maxY === minY) return padding.top + chartHeight / 2
  return padding.top + chartHeight - ((v - minY) / (maxY - minY)) * chartHeight
}
```

---

## 📋 Сводная таблица: где что рассчитывается

| Данные | Где рассчитывается | Алгоритм |
|--------|-------------------|----------|
| **Список анализов** | MongoDB (aggregation) | Группировка по `test_name`, подсчет количества |
| **Количество измерений** | MongoDB (aggregation) | `$sum` в группировке |
| **Временной ряд** | MongoDB + PostgreSQL | MongoDB: фильтрация по `test_name`, PostgreSQL: получение дат |
| **Числовые значения** | Бэкенд (Python) | Преобразование строки в float (замена запятой на точку) |
| **Минимум** | Фронтенд (JavaScript) | `Math.min(...values)` |
| **Максимум** | Фронтенд (JavaScript) | `Math.max(...values)` |
| **Среднее** | Фронтенд (JavaScript) | `values.reduce((a, b) => a + b, 0) / values.length` |
| **Тренд** | Фронтенд (JavaScript) | Сравнение первого и последнего значения |
| **Процент изменения** | Фронтенд (JavaScript) | `((lastValue - firstValue) / Math.abs(firstValue)) * 100` |
| **Отклонение от среднего** | Фронтенд (JavaScript) | `((value - avgValue) / avgValue * 100)` |
| **Дата минимума/максимума** | Фронтенд (JavaScript) | Поиск точки с min/max значением |
| **Масштабирование графика** | Фронтенд (JavaScript) | Линейное масштабирование по X и Y осям |
| **Сортировка по дате** | Бэкенд (Python) | `sort(key=lambda x: (x.get("date") is None, x.get("date") or ""))` |

---

## 🔍 Детальный анализ запросов к БД

### Запрос 1: Список аналитов

**База:** MongoDB  
**Коллекция:** `document_metadata`  
**Тип:** Aggregation Pipeline

```javascript
[
  // 1. Фильтр по пользователю
  { $match: { user_id: "uuid" } },
  
  // 2. Проекция только нужных полей
  { $project: { "extracted_data.lab_results": 1 } },
  
  // 3. Разворачивание массива lab_results в отдельные документы
  { $unwind: "$extracted_data.lab_results" },
  
  // 4. Группировка по названию анализа
  {
    $group: {
      _id: { $toLower: "$extracted_data.lab_results.test_name" },
      name: { $first: "$extracted_data.lab_results.test_name" },
      count: { $sum: 1 }
    }
  },
  
  // 5. Сортировка по названию
  { $sort: { name: 1 } }
]
```

**Производительность:** Индекс на `user_id` критичен для скорости.

---

### Запрос 2: Временной ряд анализа

**База 1:** MongoDB  
**Коллекция:** `document_metadata`  
**Тип:** Aggregation Pipeline

```javascript
[
  // 1. Фильтр по пользователю
  { $match: { user_id: "uuid" } },
  
  // 2. Проекция только нужных полей
  { 
    $project: { 
      document_id: 1, 
      "extracted_data.lab_results": 1 
    } 
  },
  
  // 3. Разворачивание массива lab_results
  { $unwind: "$extracted_data.lab_results" },
  
  // 4. Фильтр по названию анализа (case-insensitive regex)
  {
    $match: {
      "extracted_data.lab_results.test_name": {
        $regex: "^Гемоглобин$",
        $options: "i"
      }
    }
  }
]
```

**База 2:** PostgreSQL  
**Таблица:** `documents`  
**Тип:** SELECT с IN условием

```sql
SELECT id, document_date 
FROM documents 
WHERE id IN (uuid1, uuid2, uuid3, ...)
```

**Производительность:** 
- MongoDB: Индекс на `user_id` и возможно на `extracted_data.lab_results.test_name`
- PostgreSQL: Индекс на `id` (обычно primary key, уже есть)

---

### Запрос 3: Сводка по анализам документа

**База:** MongoDB  
**Коллекция:** `document_metadata`  
**Тип:** findOne с проекцией

```javascript
db.document_metadata.findOne(
  { document_id: "uuid" },
  { "extracted_data.lab_results": 1 }
)
```

**Использование:** Для проверки наличия анализов перед полной загрузкой.

---

## 🎯 Дополнительная информация на экране

### Статистические карточки

**Расположение:** `frontend/src/pages/Labs.tsx`, компонент `LineChart` (строки 92-143)

**Данные:**
1. **Минимум** - с датой измерения
2. **Максимум** - с датой измерения
3. **Среднее** - за весь период
4. **Тренд** - направление и процент изменения

### График

**Расположение:** `frontend/src/pages/Labs.tsx`, компонент `LineChart` (строки 146-418)

**Элементы:**
- Линия тренда (градиент)
- Область под линией (градиентная заливка)
- Точки данных (с hover эффектом)
- Линия среднего значения (пунктирная)
- Сетка (grid lines)
- Подписи осей

**Интерактивность:**
- Hover на точку показывает tooltip с датой и значением
- Анимация при загрузке

### Таблица значений

**Расположение:** `frontend/src/pages/Labs.tsx`, компонент `LineChart` (строки 420-483)

**Колонки:**
1. № - обратный порядок (самое новое сверху)
2. Дата - форматированная дата
3. Значение - с единицами измерения
4. Отклонение от среднего - в процентах с цветовой индикацией

**Цветовая индикация отклонений:**
- Зеленый: отклонение < 5%
- Оранжевый: выше среднего и отклонение ≥ 5%
- Синий: ниже среднего и отклонение ≥ 5%

---

## 🔄 Оптимизации и особенности

### 1. Кэширование на фронтенде

React Query кэширует результаты:
- `['labs_analytes']` - список аналитов
- `['labs_series', selected]` - временной ряд для выбранного анализа

### 2. Ленивая загрузка

Временной ряд загружается только когда выбран анализ (`enabled: !!selected`).

### 3. Фильтрация нечисловых значений

На бэкенде отфильтровываются точки, которые не могут быть преобразованы в числа:

```python
series = [p for p in series if p["value_num"] is not None]
```

### 4. Обработка отсутствующих дат

Точки без даты сортируются в конец и не отображаются на графике (фильтр `p.date`).

### 5. Нормализация числовых значений

Замена запятой на точку для корректного парсинга:
```python
float(str(v).replace(',', '.'))
```

---

## 📝 Итоговая схема потока данных

```
1. Пользователь открывает страницу /labs
   ↓
2. Фронтенд: GET /labs/analytes
   ↓
3. MongoDB: Aggregation для получения списка аналитов
   ↓
4. Фронтенд: Отображает выпадающий список
   ↓
5. Пользователь выбирает анализ (или выбирается первый автоматически)
   ↓
6. Фронтенд: GET /labs/timeseries?analyte=Гемоглобин
   ↓
7. MongoDB: Aggregation для получения точек данных
   ↓
8. PostgreSQL: SELECT для получения дат документов
   ↓
9. Бэкенд: Объединение данных, преобразование в числа, сортировка
   ↓
10. Фронтенд: Получение данных
    ↓
11. Фронтенд: Расчет статистики (min, max, avg, trend)
    ↓
12. Фронтенд: Расчет масштабов для графика
    ↓
13. Фронтенд: Рендеринг графика и таблицы
```

---

## 🐛 Потенциальные проблемы и ограничения

1. **Case-sensitive поиск:** Регулярное выражение `^analyte$` с флагом `i` делает поиск case-insensitive, но может быть медленным на больших объемах.

2. **Отсутствие индексов:** Если нет индекса на `user_id` в MongoDB, запросы будут медленными.

3. **Большое количество документов:** Если у пользователя тысячи документов, агрегация может быть медленной.

4. **Память на фронтенде:** Все точки загружаются в память, что может быть проблемой для очень длинных временных рядов.

5. **Формат дат:** Если даты в разных форматах, сортировка может работать некорректно.

---

## 🔧 Рекомендации по оптимизации

1. **Индексы MongoDB:**
   ```javascript
   db.document_metadata.createIndex({ "user_id": 1 })
   db.document_metadata.createIndex({ "extracted_data.lab_results.test_name": 1 })
   db.document_metadata.createIndex({ "user_id": 1, "extracted_data.lab_results.test_name": 1 })
   ```

2. **Пагинация временных рядов:** Если точек больше 1000, добавить пагинацию или ограничение.

3. **Кэширование на бэкенде:** Redis для кэширования часто запрашиваемых аналитов.

4. **Пакетная загрузка:** Загружать несколько аналитов одновременно, если нужно.

---

## 📚 Связанные файлы

### Фронтенд
- `frontend/src/pages/Labs.tsx` - главная страница анализов
- `frontend/src/services/documents.ts` - API сервис для работы с документами
- `frontend/src/components/Documents/LabResultsTable.tsx` - таблица результатов анализов в модальном окне

### Бэкенд
- `backend/app/api/v1/endpoints/documents.py` - эндпоинты для работы с анализами
- `backend/app/services/lab_analysis_service.py` - сервис для извлечения анализов из документов
- `backend/app/db/mongodb.py` - подключение к MongoDB

### Документация
- `docs/API_DOCUMENTATION.md` - документация API
- `docs/ТЗ/prompts/AI_PROMPT_LAB_RESULTS_EXTRACTION.md` - промпты для извлечения анализов
