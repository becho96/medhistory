# 🚀 План деплоя системы мониторинга на Production

**Дата:** 29 октября 2025  
**Сервер:** `46.21.244.23` (Яндекс.Облако)  
**Текущее состояние:** Приложение работает, мониторинг отсутствует

---

## ✅ Предварительная проверка

### Что готово:
- ✅ Production сервер доступен
- ✅ Docker установлен и работает
- ✅ Приложение развернуто на сервере
- ✅ Система мониторинга работает локально
- ✅ Все конфигурационные файлы созданы

### Что изменено:
1. **Backend:**
   - `backend/app/api/v1/endpoints/metrics.py` - новый endpoint для метрик
   - `backend/app/api/v1/router.py` - добавлен роутер метрик
   - `backend/main.py` - добавлен Prometheus instrumentator
   - `backend/requirements.txt` - добавлены зависимости мониторинга

2. **Мониторинг (новое):**
   - `monitoring/` - вся папка с конфигурациями
   - `docker-compose.monitoring.yml` - dev конфигурация
   - `docker-compose.monitoring.prod.yml` - prod конфигурация

3. **Nginx:**
   - `nginx/nginx.conf` - добавлен роут `/grafana`

4. **Скрипты:**
   - `scripts/local/start.sh` - добавлен запуск мониторинга
   - `scripts/local/stop.sh` - добавлена остановка мониторинга
   - `scripts/prod/deploy.sh` - добавлен деплой мониторинга

---

## 📋 План деплоя

### Этап 1: Подготовка (выполнить локально)

#### 1.1 Настройка переменных окружения для Grafana

Добавить в `.env.production`:

```bash
# Grafana Configuration
GF_SECURITY_ADMIN_PASSWORD=ваш_безопасный_пароль
GF_SERVER_ROOT_URL=http://46.21.244.23/grafana
GF_SERVER_DOMAIN=46.21.244.23
```

⚠️ **ВАЖНО:** Измените `ваш_безопасный_пароль` на надежный пароль!

#### 1.2 Коммит изменений (опционально)

Закоммитить все изменения в git:

```bash
git add .
git commit -m "feat: Add Prometheus + Grafana monitoring system"
git push origin main
```

Или деплоить без коммита (скрипт создаст архив текущего состояния).

---

### Этап 2: Деплой на production

#### 2.1 Запуск скрипта деплоя

```bash
./scripts/prod/deploy.sh
```

**Что произойдет:**
1. ✅ Проверка подключения к серверу
2. ✅ Проверка `.env.production`
3. ✅ Создание архива проекта
4. ✅ Загрузка на сервер
5. ✅ Создание бэкапа текущей версии
6. ✅ Остановка старых контейнеров
7. ✅ Распаковка новой версии
8. ✅ Пересборка backend контейнера (с новыми зависимостями)
9. ✅ Запуск основного приложения
10. ✅ Запуск системы мониторинга
11. ✅ Проверка статуса

**Время выполнения:** ~5-7 минут

---

### Этап 3: Проверка работоспособности

#### 3.1 Проверка основного приложения

```bash
curl -I http://46.21.244.23/health
# Ожидаемый ответ: 200 OK
```

#### 3.2 Проверка Grafana

```bash
curl -I http://46.21.244.23/grafana/
# Ожидаемый ответ: 302 Found (редирект на /login)
```

#### 3.3 Вход в Grafana

1. Откройте в браузере: `http://46.21.244.23/grafana`
2. Войдите: `admin` / ваш_пароль
3. Откройте дашборды:
   - Медицинский сервис - Бизнес метрики
   - Медицинский сервис - Системный обзор
   - Медицинский сервис - Производительность API

#### 3.4 Проверка метрик

```bash
# Подключиться к серверу
ssh yc-user@46.21.244.23

# Проверить статус контейнеров
cd ~/medhistory
docker compose -f docker-compose.prod.yml ps
docker compose -f docker-compose.monitoring.prod.yml ps

# Проверить метрики backend
curl -s http://localhost:8000/api/v1/metrics/business | jq

# Проверить Custom Exporter
curl -s http://localhost:9100/metrics | grep medhistory_users_total

# Проверить Prometheus
docker exec medhistory_prometheus wget -qO- http://localhost:9090/-/healthy

# Проверить логи
docker logs medhistory_grafana --tail 50
docker logs medhistory_prometheus --tail 50
docker logs medhistory_exporter --tail 50
```

---

## ⚠️ Возможные проблемы и решения

### Проблема 1: Backend не запускается

**Причина:** Новые зависимости не установлены

**Решение:**
```bash
ssh yc-user@46.21.244.23
cd ~/medhistory
docker compose -f docker-compose.prod.yml stop backend
docker compose -f docker-compose.prod.yml build backend --no-cache
docker compose -f docker-compose.prod.yml up -d backend
```

### Проблема 2: Prometheus не может достучаться до backend

**Причина:** Контейнеры в разных сетях

**Решение:**
```bash
# Проверить сети
docker network ls | grep medhistory

# Перезапустить мониторинг
docker compose -f docker-compose.monitoring.prod.yml restart
```

### Проблема 3: Grafana показывает "No Data"

**Причины и решения:**
1. **Datasource не создан:** Подождать 30 секунд и обновить страницу
2. **Prometheus не собирает метрики:** Проверить `http://localhost:9090/targets` на сервере
3. **Custom Exporter не работает:** Проверить логи `docker logs medhistory_exporter`

### Проблема 4: Grafana недоступна через Nginx

**Решение:**
```bash
# Перезапустить Nginx
docker compose -f docker-compose.prod.yml restart nginx

# Проверить конфигурацию
docker exec medhistory_nginx nginx -t
```

---

## 📊 Ожидаемые метрики после деплоя

| Метрика | Ожидаемое значение |
|---------|-------------------|
| Пользователи | ~14+ |
| Документы | ~520+ |
| Интерпретации | ~6+ |
| CPU Usage | 10-30% |
| Memory Usage | 50-70% |
| Disk Usage | ~40% |

---

## 🔐 Безопасность

### На production сервере:

1. ✅ **Grafana** доступна только через Nginx
2. ✅ **Prometheus** доступен только внутри Docker сети
3. ✅ **Exporters** доступны только внутри Docker сети
4. ⚠️ **Grafana пароль** обязательно изменить!

### Рекомендации:

1. **Изменить пароль Grafana** сразу после первого входа
2. **Настроить HTTPS** для безопасного доступа
3. **Ограничить доступ к Grafana** через firewall (опционально)

---

## 🎯 Чек-лист деплоя

### Перед деплоем:
- [ ] Проверить `.env.production` содержит настройки Grafana
- [ ] Установить безопасный пароль для Grafana
- [ ] Убедиться, что сервер доступен
- [ ] Сделать бэкап текущей версии (скрипт делает автоматически)

### После деплоя:
- [ ] Проверить статус всех контейнеров
- [ ] Открыть приложение и убедиться, что оно работает
- [ ] Войти в Grafana и проверить дашборды
- [ ] Проверить, что метрики собираются
- [ ] Проверить логи на наличие ошибок

### Финальная проверка:
- [ ] Основное приложение: `http://46.21.244.23`
- [ ] Grafana: `http://46.21.244.23/grafana`
- [ ] API Docs: `http://46.21.244.23/docs`
- [ ] Health check: `http://46.21.244.23/health`

---

## 📞 В случае проблем

### Откат на предыдущую версию:

```bash
ssh yc-user@46.21.244.23
cd ~
ls -la | grep medhistory_old

# Найти последнюю версию и откатиться
docker compose -f ~/medhistory/docker-compose.prod.yml down
mv ~/medhistory ~/medhistory_failed
mv ~/medhistory_old_YYYYMMDD_HHMMSS ~/medhistory
cd ~/medhistory
docker compose -f docker-compose.prod.yml up -d
```

### Просмотр логов:

```bash
# Все логи
docker compose -f docker-compose.prod.yml logs -f

# Конкретный сервис
docker logs -f medhistory_backend
docker logs -f medhistory_grafana
docker logs -f medhistory_prometheus
```

---

## ✅ Команды для быстрого деплоя

```bash
# 1. Настроить .env.production (один раз)
echo "GF_SECURITY_ADMIN_PASSWORD=your_secure_password" >> .env.production
echo "GF_SERVER_ROOT_URL=http://46.21.244.23/grafana" >> .env.production
echo "GF_SERVER_DOMAIN=46.21.244.23" >> .env.production

# 2. Запустить деплой
./scripts/prod/deploy.sh

# 3. После завершения проверить
curl http://46.21.244.23/health
curl http://46.21.244.23/grafana/

# 4. Открыть в браузере
# http://46.21.244.23/grafana
# Login: admin
# Password: your_secure_password
```

---

**Готовы к деплою?** 🚀

