# 📊 Система мониторинга медицинского сервиса

## Обзор

Система мониторинга построена на базе **Prometheus** и **Grafana** и предназначена для отслеживания бизнес-метрик и технического состояния сервиса.

## Архитектура

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Backend   │────▶│ Prometheus  │────▶│   Grafana   │
│   (Метрики) │     │  (Сбор)     │     │(Визуализация)│
└─────────────┘     └─────────────┘     └─────────────┘
       │                    ▲
       │                    │
       ▼                    │
┌─────────────┐             │
│   Custom    │─────────────┘
│   Exporter  │
└─────────────┘
       │
       ▼
┌──────────────────────────┐
│  PostgreSQL / MongoDB    │
│  MinIO / System Metrics  │
└──────────────────────────┘
```

## Компоненты

### 1. **Prometheus** (порт 9090)
- Сбор метрик с различных источников
- Хранение временных рядов
- Оценка правил алертов
- Endpoint: `http://localhost:9090` (dev), внутренний в prod

### 2. **Grafana** (порт 3000)
- Визуализация метрик
- Дашборды для анализа
- Endpoint: `http://localhost:3000` (dev), `https://your-domain/grafana` (prod)
- Логин по умолчанию: `admin` / `admin` (изменить после первого входа!)

### 3. **Custom Exporter** (порт 9100)
- Экспорт бизнес-метрик из баз данных
- Интеграция с Backend API
- Работает внутри Docker сети

### 4. **Node Exporter** (порт 9101)
- Системные метрики (CPU, RAM, Disk, Network)

### 5. **PostgreSQL Exporter** (порт 9187)
- Метрики базы данных PostgreSQL
- Количество соединений, размер БД, производительность

### 6. **MongoDB Exporter** (порт 9216)
- Метрики базы данных MongoDB
- Операции, размер коллекций, репликация

## 📈 Отслеживаемые метрики

### Бизнес-метрики

| Метрика | Описание | Источник |
|---------|----------|----------|
| `medhistory_users_total` | Общее количество пользователей | PostgreSQL |
| `medhistory_users_active` | Активные пользователи за период | PostgreSQL |
| `medhistory_documents_total` | Общее количество документов | PostgreSQL |
| `medhistory_documents_by_type` | Документы по типам | PostgreSQL |
| `medhistory_interpretations_total` | Общее количество интерпретаций | PostgreSQL |
| `medhistory_interpretations_success` | Успешные интерпретации | PostgreSQL |
| `medhistory_reports_total` | Общее количество отчетов | MongoDB |
| `medhistory_storage_bytes` | Объем хранилища MinIO | MinIO |
| `medhistory_api_requests_total` | Количество API запросов | Backend |
| `medhistory_api_request_duration_seconds` | Длительность API запросов | Backend |

### Технические метрики

- **API**: RPS, латентность, коды ответов
- **Система**: CPU, RAM, Disk I/O, Network
- **PostgreSQL**: соединения, размер БД, медленные запросы
- **MongoDB**: операции, размер коллекций, индексы
- **MinIO**: объем хранилища, операции с объектами

## 🚀 Запуск

### Development

```bash
# Запустить все сервисы включая мониторинг
./scripts/local/start.sh

# Доступ к интерфейсам
# Grafana: http://localhost:3000
# Prometheus: http://localhost:9090
```

### Production

```bash
# Деплой с мониторингом
./scripts/prod/deploy.sh

# Доступ к интерфейсам
# Grafana: https://your-domain/grafana
# Prometheus: внутренний доступ только
```

## 📊 Дашборды Grafana

### 1. Business Metrics (Бизнес метрики)
- Количество пользователей (всего, новых, активных)
- Динамика загрузки документов
- Статистика AI интерпретаций
- Использование хранилища

### 2. System Overview (Системный обзор)
- Использование CPU/RAM/Disk
- Сетевой трафик
- Доступность сервисов
- Статус контейнеров

### 3. API Performance (Производительность API)
- Request Rate (RPS)
- Response Time (P50, P95, P99)
- Error Rate
- Топ эндпоинтов по нагрузке

### 4. Database Monitoring (Мониторинг БД)
- PostgreSQL: соединения, транзакции, размер
- MongoDB: операции, коллекции, производительность
- Медленные запросы

## 🔧 Конфигурация

### Prometheus

Конфигурация: `monitoring/prometheus/prometheus.yml`

Основные настройки:
- Интервал сбора метрик: 15 секунд
- Retention: 15 дней
- Targets конфигурируются в `monitoring/prometheus/targets/*.yml`

### Grafana

Конфигурация: `monitoring/grafana/grafana.ini`

Основные настройки:
- Автоматический импорт дашбордов из `monitoring/grafana/dashboards/`
- Datasource: Prometheus (настраивается автоматически)
- Анонимный доступ: отключен

## 🔐 Безопасность

### Development
- Все порты доступны локально
- Стандартные пароли (admin/admin)

### Production
- Доступ к Grafana через Nginx с HTTPS
- Prometheus доступен только внутри Docker сети
- Обязательная смена паролей через переменные окружения:
  - `GF_SECURITY_ADMIN_PASSWORD` - пароль администратора Grafana

## 📝 API для метрик

Backend предоставляет endpoint для получения метрик:

```
GET /api/v1/metrics/business
```

Возвращает JSON с бизнес-метриками:

```json
{
  "users": {
    "total": 150,
    "active_30d": 45,
    "new_30d": 12
  },
  "documents": {
    "total": 890,
    "by_type": {
      "lab_result": 450,
      "medical_history": 200,
      "prescription": 150,
      "other": 90
    }
  },
  "interpretations": {
    "total": 320,
    "success": 310,
    "failed": 10
  },
  "storage": {
    "bytes": 1073741824,
    "objects": 890
  }
}
```

## 🛠️ Troubleshooting

### Grafana не показывает данные
1. Проверить, что Prometheus собирает метрики: `http://localhost:9090/targets`
2. Проверить datasource в Grafana: Settings → Data Sources
3. Проверить логи: `docker logs medhistory_grafana`

### Prometheus не может достучаться до targets
1. Проверить сетевую конфигурацию Docker
2. Проверить healthcheck контейнеров
3. Проверить конфигурацию targets в `prometheus.yml`

### Custom Exporter не работает
1. Проверить подключение к Backend API
2. Проверить логи: `docker logs medhistory_exporter`
3. Проверить доступность БД из контейнера exporter

## 📚 Дополнительная информация

- [Prometheus Documentation](https://prometheus.io/docs/)
- [Grafana Documentation](https://grafana.com/docs/)
- [Best Practices for Monitoring](https://prometheus.io/docs/practices/)

## 🔄 Обновление

### Обновление дашбордов
1. Экспортировать дашборд из Grafana UI
2. Сохранить JSON в `monitoring/grafana/dashboards/`
3. Перезапустить Grafana контейнер

### Добавление новых метрик
1. Добавить метрику в Backend API (`/api/v1/endpoints/metrics.py`)
2. Обновить Custom Exporter (`monitoring/exporters/custom_exporter.py`)
3. Обновить дашборды в Grafana

## 📧 Поддержка

При возникновении проблем проверьте логи:
```bash
# Все логи мониторинга
docker-compose logs prometheus grafana

# Конкретный сервис
docker logs medhistory_prometheus
docker logs medhistory_grafana
docker logs medhistory_exporter
```

