# MedHistory - Персональная медицинская история

**MedHistory** — это персональная система управления медицинской историей с AI-анализом документов. Сервис автоматически каталогизирует, структурирует и визуализирует всю медицинскую документацию пациента.

## ✨ Основные возможности

- 📤 **Загрузка документов** - поддержка PDF, JPG, PNG, DOCX форматов
- 🤖 **AI-анализ** - автоматическое извлечение метаданных через Claude VLM
- 📊 **Timeline визуализация** - интерактивная временная шкала событий
- 📑 **Генерация отчётов** - создание PDF отчётов для врачей
- 🔍 **Умная фильтрация** - поиск по специализациям, типам документов и датам
- 🔒 **Безопасность** - аутентификация и шифрование данных

## 🏗️ Архитектура

- **Frontend**: React 18 + TypeScript + TailwindCSS + Vite
- **Backend**: Python FastAPI
- **Базы данных**: PostgreSQL (структурированные данные) + MongoDB (гибкие метаданные)
- **Хранилище**: MinIO (S3-совместимое объектное хранилище)
- **AI**: OpenRouter API (Claude Sonnet 4.5)
- **Развертывание**: Docker Compose

## 📋 Требования

Перед началом установки убедитесь, что у вас установлены:

- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (версия 20.10+)
- [Docker Compose](https://docs.docker.com/compose/install/) (версия 2.0+)
- Аккаунт на [OpenRouter.ai](https://openrouter.ai/) для получения API ключа

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
cd ~/Desktop/Начало
cd "История болезни"
```

### 2. Получение OpenRouter API Key

1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
2. Пополните баланс (минимум $5)
3. Создайте API ключ в разделе [Keys](https://openrouter.ai/keys)
4. Скопируйте ключ

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cat > .env << 'EOF'
# Database Passwords
POSTGRES_PASSWORD=medhistory_secure_pass
MONGO_PASSWORD=mongodb_secure_pass

# MinIO Credentials
MINIO_ROOT_USER=minio_admin
MINIO_ROOT_PASSWORD=minio_secure_pass_123

# OpenRouter API Key (замените на ваш ключ!)
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# JWT Secret (для production используйте случайную строку)
JWT_SECRET=jwt_super_secret_key_change_in_production

# Frontend API URL
VITE_API_URL=http://localhost:8000
EOF
```

**ВАЖНО**: Замените `OPENROUTER_API_KEY` на ваш настоящий ключ!

### 4. Запуск сервиса

**Простой способ (рекомендуется):**
```bash
./start.sh
```

**Или вручную:**
```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

Сервисы будут доступны через несколько минут:

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **MinIO Console**: http://localhost:9001

### 5. Первый вход

1. Откройте http://localhost:5173 в браузере
2. Нажмите "Зарегистрироваться"
3. Создайте аккаунт с вашим email и паролем
4. Войдите в систему

## 📖 Использование

### Загрузка документов

1. Перейдите в раздел **"Документы"**
2. Перетащите файлы в зону загрузки или нажмите для выбора
3. Поддерживаемые форматы: PDF, JPG, PNG, DOCX (до 20 МБ)
4. Документы автоматически анализируются с помощью AI

### Просмотр Timeline

1. Перейдите в раздел **"Timeline"**
2. Используйте фильтры для поиска нужных документов
3. Интерактивная временная шкала показывает все события
4. Кликните на событие для просмотра деталей

### Генерация отчётов

1. Перейдите в раздел **"Отчёты"**
2. Нажмите **"Создать отчёт"**
3. Настройте фильтры (специализация, тип документа, период)
4. Сгенерируйте и скачайте PDF отчёт

## 🛠️ Управление сервисом

### Скрипты для локальной разработки

> 📁 Все скрипты находятся в `scripts/local/`, но для удобства есть короткие алиасы в корне проекта.

**Запуск:**
```bash
./start.sh                    # Первый запуск или запуск после остановки
```

**Остановка:**
```bash
./stop.sh                     # Остановить контейнеры (данные сохранятся)
```

**Обновление после изменений кода:**
```bash
./restart.sh                  # Пересобрать и перезапустить все сервисы
./restart.sh backend          # Только backend
./restart.sh frontend         # Только frontend
```

> 💡 **Подробная документация:** [scripts/local/README.md](scripts/local/README.md)

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

### Остановка сервиса (расширенное)

```bash
# Остановить без удаления данных
docker-compose stop

# Остановить и удалить контейнеры (данные сохранятся)
docker-compose down

# Полная очистка включая данные (ВНИМАНИЕ: удалит все загруженные документы!)
docker-compose down -v
```

### Перезапуск сервиса (ручной)

```bash
# Перезапустить все сервисы
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart backend
```

### Обновление кода (ручной способ)

```bash
# Остановить сервисы
docker-compose down

# Пересобрать контейнеры
docker-compose build

# Запустить заново
docker-compose up -d
```

## 🔧 Разработка

### Структура проекта

```
История болезни/
├── backend/                 # FastAPI Backend
│   ├── app/
│   │   ├── api/            # API эндпоинты
│   │   ├── core/           # Настройки и безопасность
│   │   ├── db/             # Подключения к БД
│   │   ├── models/         # SQLAlchemy модели
│   │   ├── schemas/        # Pydantic схемы
│   │   └── services/       # Бизнес-логика
│   ├── Dockerfile
│   ├── requirements.txt
│   └── main.py
├── frontend/                # React Frontend
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── pages/          # Страницы
│   │   ├── services/       # API сервисы
│   │   ├── stores/         # Zustand stores
│   │   └── types/          # TypeScript типы
│   ├── Dockerfile
│   └── package.json
├── ТЗ/                      # Техническая документация
├── docker-compose.yml       # Оркестрация сервисов
├── .env                     # Переменные окружения
└── README.md               # Этот файл
```

### Локальная разработка Backend

```bash
cd backend

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # на Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер разработки
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Локальная разработка Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Запустить сервер разработки
npm run dev
```

## 🐛 Решение проблем

### Backend не запускается

```bash
# Проверить логи
docker-compose logs backend

# Пересоздать контейнер
docker-compose up -d --force-recreate backend
```

### Frontend не загружается

```bash
# Проверить логи
docker-compose logs frontend

# Очистить кэш и пересобрать
docker-compose build --no-cache frontend
docker-compose up -d frontend
```

### Ошибка "Cannot connect to database"

```bash
# Проверить статус PostgreSQL
docker-compose ps postgres

# Перезапустить БД
docker-compose restart postgres
```

### Ошибка "OpenRouter API authentication failed"

1. Проверьте, что вы правильно указали API ключ в `.env`
2. Убедитесь, что на балансе OpenRouter есть средства
3. Перезапустите backend: `docker-compose restart backend`

### MinIO недоступен

```bash
# Проверить статус
docker-compose ps minio

# Перезапустить
docker-compose restart minio

# Войти в консоль MinIO
# URL: http://localhost:9001
# Login: minio_admin
# Password: minio_secure_pass_123
```

### Порты заняты

Если порты 5173, 8000, 5432, 27017, 9000, 9001 уже используются:

1. Остановите конфликтующие сервисы
2. Или измените порты в `docker-compose.yml`

## 📊 Мониторинг

### Доступ к базам данных

**PostgreSQL:**
```bash
docker exec -it medhistory_postgres psql -U medhistory_user -d medhistory
```

**MongoDB:**
```bash
docker exec -it medhistory_mongodb mongosh -u admin -p mongodb_secure_pass
```

**MinIO Console:**
- URL: http://localhost:9001
- Username: `minio_admin`
- Password: `minio_secure_pass_123`

### Использование ресурсов

```bash
# Статистика контейнеров
docker stats

# Размер данных
docker system df -v
```

## 💰 Стоимость использования

### OpenRouter API

Модель Claude Sonnet 4.5:
- ~$3 за 1M входных токенов
- ~$15 за 1M выходных токенов

**Примерный расчет:**
- Анализ одного документа: ~$0.02-0.05
- Генерация отчёта: ~$0.10-0.20
- При 100 документах: ~$5-10 в месяц

**Рекомендация:** Начните с $10 на балансе для тестирования.

## 🚀 Production развертывание

### Быстрый старт в облаке

Хотите развернуть MedHistory в **Яндекс.Облаке**?

📖 **[Быстрый старт (10 минут)](./QUICKSTART_PRODUCTION.md)** - пошаговое руководство  
📚 **[Полная документация](./DEPLOYMENT.md)** - детальное руководство со всеми вариантами

### Основные отличия production от development

```bash
# Production версия использует:
docker compose -f docker-compose.prod.yml --env-file .env.production up -d
```

**Отличия:**
- ✅ Multi-stage build для frontend (оптимизация размера)
- ✅ Nginx reverse proxy с SSL поддержкой
- ✅ Production режим без hot reload
- ✅ Несколько workers для FastAPI
- ✅ Rate limiting и безопасность
- ✅ Логирование и health checks
- ✅ Оптимизация ресурсов

### Скрипты для автоматизации

```bash
# Настройка нового сервера
./scripts/setup-server.sh

# Автоматический деплой
./scripts/deploy.sh <server_ip> <ssh_user>

# Создание бэкапа
./scripts/backup.sh ~/backups

# Восстановление из бэкапа
./scripts/restore.sh ~/backups/medhistory_backup_*.tar.gz
```

## 🔒 Безопасность

### Development (локальная разработка)

Для локальной разработки достаточно стандартных паролей из `.env`.

### Production deployment

**КРИТИЧЕСКИ ВАЖНО** перед развертыванием:

1. **Создайте `.env.production`** на основе `.env.production` (шаблон)
2. **Измените ВСЕ пароли** на длинные случайные строки (минимум 16 символов)
3. **Сгенерируйте JWT_SECRET**: `openssl rand -hex 32`
4. **Настройте HTTPS** через Let's Encrypt (бесплатно)
5. **Настройте firewall** - откройте только 80, 443, 22
6. **Настройте автоматические бэкапы** (см. скрипты)
7. **Включите мониторинг** через Яндекс.Мониторинг

### Бэкапы

```bash
# Автоматический бэкап всех данных
./scripts/backup.sh ~/backups

# Настройка автоматических бэкапов (каждый день в 3:00)
crontab -e
# Добавьте: 0 3 * * * cd ~/medhistory && ./scripts/backup.sh ~/backups

# Восстановление
./scripts/restore.sh ~/backups/medhistory_backup_20241019_030000.tar.gz
```

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все контейнеры запущены: `docker-compose ps`
3. Проверьте документацию в папке `ТЗ/`

## 📝 Лицензия

Этот проект создан для личного использования.

## 🎯 Roadmap

### MVP (текущая версия)
- ✅ Загрузка и хранение документов
- ✅ AI-анализ через OpenRouter
- ✅ Timeline визуализация
- ✅ Генерация PDF отчётов

### Будущие улучшения
- 🔄 Извлечение структурированных данных из анализов
- 📈 Графики динамики показателей (гемоглобин, холестерин)
- 🔍 Полнотекстовый поиск по документам
- 👨‍👩‍👧‍👦 Семейные аккаунты
- 📱 Мобильное приложение

---

**Версия:** 1.0.0  
**Дата:** Октябрь 2025

Удачного использования! 🎉

