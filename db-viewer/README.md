# DB Viewer

Простой веб-интерфейс для просмотра содержимого PostgreSQL таблиц в локальном Docker.

## Быстрый запуск

1. Убедитесь, что локальный Docker запущен с базой данных:
```bash
cd ..
docker compose -f docker-compose.base.yml -f docker-compose.local.yml --env-file environments/local.env up -d
```

2. Установите зависимости и запустите:
```bash
cd db-viewer
pip install -r requirements.txt
python app.py
```

3. Откройте в браузере: http://localhost:5050

## Конфигурация

По умолчанию используются настройки из `environments/local.env`:
- Host: `localhost`
- Port: `5432`
- Database: `medhistory`
- User: `medhistory_user`
- Password: `medhistory_local_pass`

Можно переопределить через переменные окружения:
```bash
DB_HOST=localhost DB_PORT=5432 DB_NAME=medhistory DB_USER=user DB_PASSWORD=pass python app.py
```

## Возможности

- 📋 Просмотр списка всех таблиц
- 📊 Количество записей в каждой таблице
- 📝 Информация о колонках (имя, тип)
- 🔍 Просмотр данных с пагинацией
- 🎨 Подсветка типов данных (UUID, даты, boolean)

