# Обновлено для совместимости с mcp (anyio>=4.5) — starlette 0.31+ требуется.
# fastapi/starlette запинены: starlette 1.x ломает prometheus-fastapi-instrumentator==6.1.0
# (route.path на _IncludedRouter → 500 на всех /api/v1/*). Держим на 0.115.x + starlette 0.41.x.
fastapi==0.115.6
starlette==0.41.3
uvicorn[standard]>=0.30.0
pydantic[email]==2.13.4
pydantic-settings==2.13.1
email-validator==2.1.0

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
psycopg2-binary==2.9.9
motor==3.3.2
pymongo==4.6.0
pgvector==0.3.6

# MinIO S3
minio==7.2.0

# File processing
Pillow==10.1.0
PyPDF2==3.0.1
python-docx==1.1.0
python-multipart==0.0.27

# Authentication
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.27

# HTTP client (совместим с anyio 4)
httpx>=0.27.0

# PDF generation
reportlab==4.0.7

# Utils
python-dotenv==1.0.0
python-dateutil==2.8.2

# Monitoring
prometheus-client==0.19.0
prometheus-fastapi-instrumentator==6.1.0

# AI Assistant — LangGraph multi-agent
# Pinned to versions tested in local dev container — loose >= constraints
# made pip resolver backtrack indefinitely between 1.x and 0.3.x lines.
langgraph==1.1.6
langchain==1.2.15
langchain-core==1.2.28
langchain-anthropic==1.4.0
langchain-openai==1.1.12

# MCP Server (Patient Data Tools)
mcp==1.27.0

